[project]
name = "nakabandi-api"
version = "0.0.0"
description = "NAKABANDI API: modular monolith (DOC 2 §2.6)."
requires-python = ">=3.11,<3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.12,<0.13"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "nakabandi"
