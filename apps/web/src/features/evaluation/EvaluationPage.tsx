/**
 * EvaluationPage.tsx — stub for C1. Also used for /outbox and /audit stubs.
 * Full implementation lands in Step C7.
 */
export default function EvaluationPage() {
  return (
    <div className="nk-stub-page">
      <h2>Evaluation / Ops / Outbox / Audit</h2>
      <p>Evaluation, ops, outbox and audit pages — implemented in Step C7.</p>
    </div>
  );
}
