# Names only, no values. Copy to .env locally; .env is git-ignored.
# Bank gateway simulator (DOC 3, apps/bank-sim config.ts). API settings names are added at Step A2.
WEBHOOK_SECRET=
API_BASE_URL=
API_SERVICE_KEY=
SIM_STATUS_URL=
# Bank-sim console login (apps/bank-sim)
CONSOLE_USER=
CONSOLE_PASS=
PORT=
