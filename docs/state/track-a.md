# TRACK A — Platform & Integration
OWNER:            Systems lead + coding agent (Integration Owner)
CURRENT_STEP:     A12 — Case Bundling & Evidence Pack: agent side DONE, see LAST_COMPLETED and PAUSED_AT. A12 is Track A's last build step (A1-A12).
LAST_COMPLETED:   A12 — Case Bundling & Evidence Pack. Built casework/ (S1: domain/{case.py, bundle.py, brief.py}, application/{ports.py, use_cases.py: BundleCluster/GetCase/ListCases}, infrastructure/{models.py table `cases`, case_repo.py}, interfaces/routers.py: GET /clusters, /clusters/{id}, /cases, /cases/{id}; S2: evidence/{bundle.py EvidenceBundle, hashing.py hash_report, certificate.py Section63Draft, pdf.py ReportLab render_pdf, file_store.py LocalFileStore, models.py table `evidence_packs`, repo.py, service.py BuildEvidencePack}, interfaces routes POST /alerts/{id}/evidence-pack, GET /evidence-packs/{id}(/download)). Facade `casework.CaseService` (bundle/get/list/build_evidence_pack/get_evidence_pack/download_evidence_pack/register_subscribers), wired into main.py (case_service_factory, register_casework in bus_registrars, router registered). New policy key config/policy.yaml `casework.bundle_debounce_min: 60` (DOC3 S1 "once per cluster per sim hour"). New Settings: evidence_store_path, evidence_font_path (both optional, safe defaults). New runtime dep: reportlab (apps/api/pyproject.toml, uv.lock updated).
  R3 not landed (docs/reference/bsa_s63_schedule.md does not exist): per DOC4's own precedent for this exact situation (Track D's D4 "draft against DOC3's own section list before R3 lands; revise once R3's Schedule text arrives"), Section63Draft is built from DOC3 S2's own field list, labelled DRAFT, and is NOT presented as matching the official Schedule form. Sync 8 (R3 -> Section63Draft field labels, Systems lead + Designer) is still open and must be re-checked once R3 lands.
  Two small, deliberate additive touches outside Track A's own Ownership Map, both flagged here rather than silently made: (1) graph (Track B, done/merged) gained `ClusterService.account_ids(cluster_id)` / `ClusterRepo.get_account_ids` — a pure read over the existing `cluster_members` table (DOC2's own table lists it as feeding "M2, S1"), no behavior change to anything else. (2) intake (Track A's own, so no real cross-track concern) gained three bulk read methods on LienContextLookup: `accounts_by_ids`, `complaints_for_accounts`, `hops_among`.
  Evidence: `npm run ci:py` — ruff check clean, ruff format clean, pyright 0 errors (1 pre-existing networkx warning, unrelated), import-linter 19/19 contracts kept (including the new "casework uses other modules only through their facades"). pytest: 664 tests (649 baseline + 15 new: apps/api/tests/casework/{test_domain.py, test_evidence.py} 11 unit tests, apps/api/tests/golden/test_casework_golden.py 4 tests). All 15 new tests pass. DONE WHEN, each independently verified: "two complaints on one cluster give one case with both" (test_two_complaints_on_one_cluster_give_one_case_with_both, real app/ingest/pipeline, no bypass); "hash reports reproduce" (test_hash_report_is_reproducible, same input bytes -> identical HashReport twice); "a tampered audit row breaks verify and mismatches the pack's head hash" (test_tampered_audit_row_breaks_verify_and_mismatches_the_packs_head_hash: UPDATE one audit_entries row -> GET /audit/verify ok=False and head_hash != the pack's stored audit_head_hash); "non-LEA roles get masked refs" (test_non_lea_role_gets_masked_refs_in_the_case + test_evidence_pack_versions_and_masks_for_non_lea: district_officer's case accounts and evidence-pack PDF both carry `mask_ref`-masked refs, i4c_analyst's do not). One generated sample PDF was produced and reviewed (ReportLab, %PDF header, ~3.7 KB for a small fixture).
  A REAL BUG FOUND AND FIXED along the way (by the golden test, not by the unit tests, which all stayed green throughout): `event_bus_for(session)` doesn't just build an EventBus, it WIRES every `bus_registrars` entry onto it (registers every module's subscribers) — an adapter that eagerly calls `app.state.event_bus_factory(session)` during ITS OWN construction, while itself being constructed from inside a bus_registrar, recurses infinitely (`RecursionError`). `wiring.CaseworkClusterSource` did exactly this. Fixed: it only ever does reads (`context_for`, `account_ids`, neither publishes), so it now takes a plain unregistered `EventBus()` instead of the factory. Also found and fixed: `BuildEvidencePack` captured the audit head hash BEFORE appending its own "evidence_pack.created" audit entry, so the value stored on the pack was always one entry stale — `GET /audit/verify` right after a build would never match it even with no tampering at all, which would have made "mismatches the pack's head hash" untestable (any post-build state already mismatched). Fixed: append first, then re-read verify().head_hash for the stored value, so the pack's stored hash is the chain's state AT the moment it was sealed, and only a real tamper moves it.
  Honest limits and gaps, not blocking the Done When above: (1) GET /clusters and /clusters/{id} are simplified — `status` is always "active" (the `clusters` table's own absorbed_by/absorbed_at isn't surfaced through graph's facade) and `novelty` is always 0.0 (not wired to the forecast facade); neither is covered by A12's Done When, both are usable stubs Track C's C6 can build against. (2) `victim_count` = `complaint_count`: the schema has no victim/complainant identity separate from a complaint (ComplaintModel has no victim_id), so "one victim per complaint" is the honest count, not a distinct-victim tally the data can't support. (3) Evidence-pack masking is decided ONCE, by the BUILDING principal's role, baked into the immutable PDF (packs can't be re-rendered per-viewer without breaking "the PDF's own SHA-256 stored outside the PDF"); a bank_nodal or lower-privilege viewer later downloading an i4c_analyst-built pack would see whatever that builder's role allowed. (4) No Devanagari font is bundled (offline sandbox, no network access to fetch one, and S5's Hindi content doesn't exist yet either): `render_pdf` falls back to Helvetica and logs a warning; `Settings.evidence_font_path` is the hook for whoever adds a real font file. (5) Cluster-graph endpoints cap at 200 nodes (DOC3 S1 edge case) but edges are only counted among the capped node set, so a very large cluster's edge list is a strict subset, not exhaustive.
STATUS:           CHECKPOINT
PAUSED_AT:        [SECOND RESUME, ALSO COMPLETED] User then asked, one by one: (a) wire the category/archetype CSVs into the generator, (b) actually run TrainModels, (c) make System Integrity/Evaluation real, skip guided_demo. All done:
  (a) DONE, fully: real_seed.py gained `load_category_weights()` (real MHA/NCRP-sourced shares for the 4 locked ComplaintCategory values, renormalised — the CSV has 16, the contract only allows 4, mapped: investment_scam<-investment_fraud, task_job_scam<-job_fraud); generator.py's category draw is now weighted by it when present (World gained a `category_weights` field, still pure/no I/O — cli.py's `_build_world` does the CSV read). clusters.py's bridge-account bug (fixed pair clusters[0]/[1] only, never scaling with n_clusters=20) also fixed: now every cluster from the 2nd on independently rolls a bridge_rate chance to link to an earlier one. Archetype CSV (mule_cluster_archetypes.csv) full wiring into per-cluster account-count/lifetime/channel-mix was NOT attempted — same reasoning as guided_demo, a bigger design-shaped task; flagged, not done.
  (a, cont'd — world-sim fixes #7/#9/#10/#2/#3 from the first resume) were completed earlier this same continuation: inject_cluster's uuid.uuid4() replaced with a seeded id (also fixed the cascading non-determinism in its own rng); runner.py's LiveRunner.inject_cluster no longer generates a second, mismatched id; /control/reset now actually rebuilds the World (registry+clusters) on a new seed instead of silently continuing on the old one; oracle_api reads run_id live off the shared ClockState instead of a copy frozen at boot (fixes GET /oracle/clusters returning empty after any reset+restart); infra/Dockerfile.worldsim now COPYs data/ so the deployed/Docker image stops silently falling back to synthetic registry data.
  (b) DONE and genuinely working end-to-end: TrainModels had NO real TrainingDataPort implementation at all before this (only the abstract class + a docstring describing one). Built one for real: `forecast/infrastructure/training_data.py`'s SqlTrainingDataPort reads through graph/intake/geo's facades only (no LC-10 violations), reusing/mirroring live_pipeline.py's graph-ClusterContext -> forecast-ClusterContext adapter (the two ClusterContext classes are deliberately different shapes). Added 2 small additive read methods to intake.LienContextLookup (`complaints_up_to`, `first_cashout_after`) and one to graph (`ClusterService.all_account_ids` / `ClusterRepo.get_all_account_ids`, ABC + its one real implementation, no stubs existed to update) — all facade-only, no contract changes. `scripts/train.py` (new, mirrors reset.py's pattern) actually runs it: first live run against the real accumulated DB produced 19,860 train rows / 4,523 val rows in 14s, a real persisted hgb_v1 model (models/scorer_latest.joblib, Brier 0.0063). ALSO had to wire ModelStore into main.py's live pipeline at all (`app.state.model_store`, new `Settings.model_store_dir`, threaded through `pipeline_for`) — GenerateForecast has always accepted a model_store param but main.py never constructed or passed one, so a trained model could never have been picked up by live serving even if one existed. This broke 15 integration/golden tests the moment a real model file existed on disk (their tiny fixtures' confidence dropped below the alert floor against a model trained on the full registry) — fixed by isolating every test's model store to an empty per-test tmp_path (NAKABANDI_MODEL_STORE_DIR), matching the existing DATABASE_URL-per-test isolation pattern; found and fixed one test with its own bespoke create_app() call that the first sweep missed. Full apps/api/tests suite verified green again after (577/577) before moving on.
  (c) DONE, honestly scoped: evaluation.run_experiment()'s own docstring admits its world-generation and pipeline-injection steps are STILL STUBBED ("B8/B9 swap point", not reached) — that path could not have produced real numbers no matter what I wired. Built `scripts/evaluate.py` instead: an offline script (never imported by main.py — respects "main.py may never import nakabandi.evaluation" and "only evaluation may import the oracle client", both literally still true, since this is a script not the live process) that scores the trained model's real candidate rankings (reusing SqlTrainingDataPort + forecast's own score_candidates()) against real resolved complaints, computing real hit_rate@k/precision@k/brier_score via evaluation.metrics (the "fully tested, not stubbed" half of the harness). First real run: hit_rate@5 = 10.2%, precision@5 = 2.4%, Brier 0.006, n=226 complaints / 37,168 candidate rows — genuinely modest numbers (this is a first-pass model against ~200 candidates per complaint), not the old fixture's fake 61-94%, and that's the point. Output written to apps/web/public/eval-results.json (Vite serves public/ verbatim; confirmed present in `npm run build`'s dist/ output too). Rewrote useEvaluation.ts to fetch it with an honest null/empty state (not caught in a try/catch that could silently show stale data), and rewrote EvaluationPage.tsx to show only the metrics actually computed — dropped compare-mode/feedback-plot/cold-start-chart entirely (deleted the now-fully-dead ColdStartChart.tsx) rather than leave them wired to data that doesn't exist. Rewrote EvaluationPage.test.tsx to match; full frontend suite verified 116/116 after.
  guided_demo: skipped per explicit instruction, untouched.
  Final full verification after everything in this entry: apps/api/tests + apps/world-sim/tests together 651/652 (the one failure is the SAME pre-existing test_simconfig_loads n_clusters mismatch from before any of this session's edits, never caused by anything here); apps/bank-sim npm test 14/14 (unchanged from the first resume); apps/web npx vitest run 116/116; `npm run build` succeeds. ruff/pyright/import-linter (19/19 contracts) all clean across every touched API file.

_FIRST_RESUME_ENTRY_BELOW_ (superseded by the above but still an accurate record of that session's own fixes):
PAUSED_AT_FIRST_RESUME: All world-sim fixes #6,#7,#9,#10 plus #2 (oracle run_id) and #3 (Dockerfile data/ copy) from the list below are now DONE (edited, ruff+pyright clean, apps/world-sim/tests 74/75 passed — same single pre-existing unrelated failure as before, `test_simconfig_loads` n_clusters 6-vs-20). All 3 bank-sim findings (CONSOLE_USER/PASS required, SESSION_SECRET added, reject handler routed through a new rejectLien()) are DONE — tsc clean, `npm test` 14/14 passed. NOT fixed, deliberately (design-gap/bigger items, flagged not attempted): #4 category-from-CSV wiring, #5 bridge-account scaling + archetype CSV wiring, #8 guided_demo wiring, #1 and #11 (cosmetic/low-impact, noted only).

  CORRECTED PLAN on the "data-scraping prompt" the user asked for: investigated `TrainModels`/`TrainingDataPort` (apps/api/src/nakabandi/forecast/application/train.py) before writing anything, and the original plan (a flat feature-CSV schema for a scraping agent) was WRONG — training data is NOT a CSV a scraping agent hands over. `TrainModels.run()` fetches `complaint_records()`/`delay_records()` straight from THIS system's own database (real ClusterContext/Candidate/actual_location_id rows built from already-ingested complaints+cashouts), the same rows world-sim's live/history run already produces. The actual bottleneck is (a) nobody has ever invoked `TrainModels` in this environment, and (b) `data/seed/complaint_category_priors.csv` / `mule_cluster_archetypes.csv` / `state_cybercrime_weights.csv` — already real, well-sourced (NCRP/MHA/I4C-cited) statistics, mostly NOT placeholder — sit unused by generator.py (world-sim findings #4/#5 above), so the simulated volume that would exist to train on is less realistic than it should be. Told the user this directly rather than handing over a schema that would produce data the system can't actually consume; gave the volume guidance instead (policy.forecast.timing.n_min=8 is the bare floor per cluster for the timing model to fit at all; recommended several hundred to ~1000+ resolved complaints, i.e. run `worldsim history`/a long live run for enough simulated days) since that part of the question — "how much data" — still had a real, useful answer even though "what schema" did not.

  Old entry below, superseded except for the world-sim/bank-sim finding numbers it still documents accurately:
PAUSED_AT_OLD: User asked for a full audit-and-fix pass across world-sim, bank-sim and the web app ("there are things that appear hardcoded... audit everything, fix whatever's wrong") after noticing expired-looking alerts and suspicious confidence values. Two background Explore agents completed full audits (findings below, NOT yet triaged into this file's Learnings — read the agent hand-back reports if still in scrollback, otherwise re-derive by re-reading the files named below, the findings are reproducible). Frontend fixes are DONE, tested, and committed to the working tree (not git-committed — nothing this session has been git-committed, only edited). World-sim has 2 of ~10 findings fixed. Bank-sim has 0 of ~5 findings fixed (not started). The data-scraping prompt for model training data was NOT YET WRITTEN — still owed to the user. Ran out of session budget mid-way through fixing world-sim finding #7 (inject_cluster's non-seeded uuid) — no edit was made for that one, only investigated (rng.py read, fix plan decided: seed cluster_id from `rng_for(cfg.seed, "inject_cluster", f"{district_id}:{sim_now}")` instead of `uuid.uuid4()`, since the function's own internal rng is ALSO currently (wrongly) derived from the random id, cascading the non-determinism into the cluster's footprint/timing perturbation too).

  DONE this session (verified: ruff/pyright/import-linter/pytest 577-577 passed for API changes; eslint/tsc/vitest 119/119 passed for web changes; world-sim changes only syntax-compiled, not test-run — apps/world-sim has its own test suite at apps/world-sim/tests/, not yet re-run after the 2 edits below):
  - API: apps/api/src/nakabandi/intake/interfaces/routers.py + alerting/interfaces/stream.py — POST /ingest/tick now publishes a real `sim.time` SSE event (was: nothing ever published it despite the frontend listening and a stale comment claiming the heartbeat did; heartbeat is unchanged, still dataless keepalive).
  - API: apps/api/src/nakabandi/casework/evidence/pdf.py — full rewrite from json.dumps-into-Preformatted-blocks to a real formatted report (tables, prose, bullets); also added a previously-missing "Officer actions / Recorded outcomes" section the old renderer silently dropped. XML-escapes all dynamic content before it reaches a `Paragraph` (docstring explains why).
  - Dev DB: ran `scripts/reset.py` once this session to fix a schema-drift crash (`intercept_assessments.best_unit_lat` missing — the uncommitted interception diff added the column to the ORM model but the on-disk sqlite file predated it). Not needed again unless the schema changes further.
  - Web: apps/web/src/features/map/useHeatmap.ts — removed `FIXTURE_HEATMAP`/`FIXTURE_HOTSPOT_ALERTS` (fake Delhi/Mumbai/Gurugram hotspot data with fake alert IDs ALT-2026-00x) and the "empty real response silently swapped for fake data" pattern (DOC1 §1.0 violation — Sync 4 landed long ago, this was a stale stub past its swap point). `useHeatmap` now returns real data, an honest empty state, or a real `isError`; no more invented data.
  - Web: apps/web/src/features/map/MapPage.tsx — `handleOpenHotspot` (the map's hotspot-click drawer) used to ALWAYS show fake `FIXTURE_HOTSPOT_ALERTS` regardless of which real cell was clicked, undisclosed as fake in the UI. Now filters the already-fetched real `alerts` array by `target.id === cell.id` (exact match, correct for location-resolution hotspots; cell/district-aggregate clicks correctly fall through to the drawer's pre-existing "no active alerts linked" empty state rather than inventing a list, since there's no real per-cell alert attribution available from the API without a Contract Change).
  - Web: apps/web/src/features/dashboard/DashboardPage.tsx — same FIXTURE_HEATMAP fallback removed from the KPI tile calc (`cells = heatmapData?.cells ?? []`).
  - Web test: apps/web/src/features/map/__tests__/MapPage.test.tsx — rewritten to mock `apiClient.GET` directly (matching the AlertDetail.test.tsx/ClusterGraph.test.tsx pattern) instead of implicitly relying on the now-removed production fixture; discovered along the way that the old "Real-time active forecast intensity window (p120)" legend note text the test asserted on NEVER EXISTED in real backend output (`analytics/application/queries.py`'s real `LIVE_NOTE` is "Expected cash-out mass behind alerts raised in this window.") — this whole test suite had only ever exercised the fixture path, never the real integration path; test now asserts the real string.
  - Web: confirmed `apps/web/src/features/map/useRegions.ts`'s similar-looking `DEMO_REGIONS` fallback is NOT the same bug class — it's static administrative geography (state/district names/ids/centroids), legitimate offline-bundled reference data per DOC2 §2.7, not simulated/forecast data. Left as-is, deliberately.
  - World-sim: apps/world-sim/src/worldsim/core/observe.py — `observe()` took an `rng` param from callers who seed it uniquely per tick/day (runner.py "live_observe"+tick, cli.py "observe_step"+day) but never used it, instead reseeding internally keyed ONLY by event count (`rng_for(cfg.seed, "observe", f"{n}")`) — any two calls with the same event count drew the identical lag sequence. Now uses the passed-in `rng` directly.
  - World-sim: apps/world-sim/src/worldsim/core/scenarios.py — `inject_cluster`'s channel-mix builder used `getattr(cfg.channels, ch.lower(), 0.33)`, but `ChannelsConfig` only has a `.mix: dict` field (no `.atm`/`.branch`/`.agent` attrs), so it always missed and gave every injected cluster a flat 33/33/33 split regardless of the configured 65/20/15 ATM-heavy mix. Fixed to `cfg.channels.mix.get(ch, 0.33)`.
  - Both world-sim edits: `py_compile`-checked only (syntax-valid); world-sim's own pytest suite (apps/world-sim/tests/) has NOT been re-run against these two changes yet — do that first when resuming, before touching anything else, in case either fix needs adjustment.

  NOT YET DONE — world-sim (full findings from the audit agent, file:line still accurate as of this write):
  - #2 [BUG] cli.py:263,280 vs control_api/__init__.py:103 — two different run_ids for one live session (`/control/start` overwrites `clock.run_id` with a fresh uuid even on first start, while oracle_api's `_run_id` was fixed at the OLD value at process boot) — breaks `GET /oracle/clusters` (evaluation's hidden-truth source) on every live run.
  - #3 [BUG] cli.py:51 + infra/Dockerfile.worldsim — `load_real_registry(Path("data/seed"), ...)` is CWD-relative; Dockerfile.worldsim never `COPY`s `data/` into the image, so the deployed/Docker system ALWAYS falls back to synthetic registry data, silently, never the curated real bank/location data. Fix: add `COPY data/ data/` to infra/Dockerfile.worldsim (Track A's own file — safe/appropriate for me to fix directly next session, no Track B coordination needed).
  - #4 [DESIGN-GAP] generator.py:179-180 — complaint `category` is uniform-random over 4 hardcoded categories, ignoring `data/seed/complaint_category_priors.csv` (16 real MHA-sourced categories/shares/amounts/channels) entirely; the 4 hardcoded category_ids don't even match the CSV's ids. Bigger wiring task, not attempted.
  - #5 [DESIGN-GAP] clusters.py:222-243 — bridge accounts (`network.bridge_rate`) only ever link the fixed pair clusters[0]/clusters[1], never scaling with `n_clusters` (20); also `data/seed/mule_cluster_archetypes.csv` (4 real archetypes) is completely unused by build_clusters(). Bigger wiring task, not attempted.
  - #7 [BUG] scenarios.py:61 `inject_cluster`'s `cluster_id = f"INJ-{uuid.uuid4().hex[:8]...}"` is NOT seeded from cfg.seed — breaks "fully seedable and reproducible" (DOC1 M1). Worse: the function's own `rng = rng_for(cfg.seed, "inject_cluster", cluster_id)` is keyed off that same random id, so footprint/timing perturbation for an injected cluster is ALSO non-reproducible as a result. Fix decided, not applied (see PAUSED_AT intro above).
  - #8 [BUG] guided_demo_script() (scenarios.py:177-179) is dead code — nothing calls it; `--scenario guided_demo` behaves identically to `free`. If ever wired up it would ALSO misbehave: the T+6h inject_cluster step uses `locality: "urban"` (not a valid Literal value) and `district_id: "MP-BHOPAL"` (outside the 4 demo states UP/MH/HR/JH), which would place the cluster at Null Island (0,0). Deliberately NOT attempted — this is a real feature-design decision (how the scripted demo narrative should fire events), Track B's call, flagged with a clear recommendation instead of a rushed fix.
  - #9 [BUG] runner.py:151-188 `LiveRunner.inject_cluster()` generates its OWN `cluster_id` via a second independent `uuid.uuid4()` call and discards the id `scenarios.inject_cluster()` actually returns (and which becomes the cluster's real `.id` in `world.clusters`) — the id reported to the API caller and saved to the truth store never matches the id actually on the cluster object.
  - #10 [BUG] control_api/__init__.py:127-153 `/control/reset` updates `clock.seed`/counts and wipes the truth store, but never rebuilds `_world` (registry+clusters) — actual generation silently keeps running on the ORIGINAL seed forever; also never clears injected clusters. Contradicts DOC3's own edge-case table ("restores seeded state... wipes world.db").
  - #1 [COSMETIC] clusters.py:116,230 — a comment has the copy-direction backwards relative to the code (behavior seems intentionally fine, just mislabeled).
  - #11 [minor, note only] generator.py:141-196 — a complaint can be `is_innocent=False` with `cluster=None` when zero clusters are currently active; behaves like noise downstream but is labeled otherwise. Low practical impact at n_clusters=20, flagged not fixed.

  NOT YET DONE — bank-sim (apps/bank-sim/, Track C; audit agent ran `npm test` clean, 14/14, before finding these — re-run after any fix):
  - [security] src/config.ts:81-84 — CONSOLE_USER/CONSOLE_PASS use insecure defaults (`bank_nodal`/`change-me`) via `.optional()`, unlike every other secret in the file which is `.required()` (fails fast if unset). A deployment that forgets to set them boots reachable with a public default login for the lien-approval console. Fix: make both `.required()` to match the sibling pattern.
  - [security] src/server.ts:55-62 — express-session's cookie-signing `secret` reuses `config.webhookSecret` (the HMAC key that authenticates inbound webhooks) — conflates two trust boundaries; a leak of either weakens both. Fix: add a separate required `SESSION_SECRET` env var.
  - [bug] src/routes/console.ts:146-174 `POST /console/requests/:id/reject` — unlike /apply and /release (which route through liens.ts, SELECT-then-validate, throw if not found/not pending), reject does an unconditional blind `UPDATE ... WHERE request_id=?` (silent no-op if missing) then ALWAYS fires a real callback POST to the live API, even for a nonexistent id or one already applied/released — corrupts bank-sim's own local status history and sends spurious callbacks (the API's own domain layer has a forward-only-transition guard so it isn't corrupted downstream, but bank-sim's local state and the extra network traffic are still wrong). Fix: mirror applyLien/releaseLien's lookup-and-validate pattern.
  - [design-gap, low priority] src/server.ts:23-29 — `AppOptions.disableAutoRelease` is declared and passed by both test files but never read by `createApp()` (only `main()`, which tests never call) — currently harmless (dead option) but misleading for a future refactor. Either wire it in or remove it.
  - [spec-note, no action needed] src/routes/webhook.ts — LC-6 lists `Idempotency-Key` as required but the handler treats a missing one as fine (no dedup, no rejection) — may be intentional leniency; flagged as a triage decision for whoever owns LC-6, not changed.
  - Bank-sim signature verification, timestamp window, hold_request re-validation, console DB persistence, callback shape vs. the API's Pydantic model, retry/backoff, and secret handling were all VERIFIED CORRECT by the audit — do not re-investigate those.

  NOT YET DONE — owed to the user directly, not started:
  - A written prompt (with exact CSV column-name schema and a recommended row-count range) for the user's own web-scraping/synthesis agent to produce training data for (a) the location-scoring HistGradientBoostingScorer (apps/api/src/nakabandi/forecast/domain/scorers.py — needs `ModelStorePort`-loadable trained model; currently always "fallback"/heuristic because none exists) and (b) enough per-cluster cash-out delay observations to clear `policy.forecast.timing.n_min=8` so MixtureTimingModel.fit() actually fires instead of using un-fit policy-default priors. Needs: figuring out exactly what feature columns GenerateForecast/HistGradientBoostingScorer.fit(train_rows, labels) expects (apps/api/src/nakabandi/forecast/domain/scorers.py, FeatureRow type) before writing the schema — not yet looked at this session.
  - A compiled list of "other constraints genuinely pulling the site down" with suggested fixes for ones I can't do directly. Partial list already known from this session, not yet written up for the user: (1) world-sim's `guided_demo` scenario is dead/non-functional — recommend either wiring it properly (design decision, Track B) or removing the CLI option so it stops implying a feature that doesn't exist; (2) running the live demo at speed=60 (my own choice this session, for fast backend verification) makes alerts expire within ~75 real seconds of appearing — far too fast for a human to actually interact with in the UI (acknowledge/dispatch/etc.); recommend a much lower speed (1-5x) for any actual interactive/human demo, reserving 60x for automated verification only; (3) the evaluation harness / System Integrity page is 100% hardcoded frontend fixture data with zero backend endpoint behind it (apps/web/src/features/evaluation/api/useEvaluation.ts's own comment admits this candidly) — this is a bigger, honest, disclosed gap (not hidden like the heatmap one was), needs a real `evaluation-runs` API route + wiring, flagged in DOC4 already, not attempted; (4) infra/Dockerfile.worldsim missing `COPY data/ data/` (see world-sim #3 above) silently degrades the deployed/Docker system to fully-synthetic data.
  - World-sim's own test suite (apps/world-sim/tests/) has not been run this session at all — run it before/after any further world-sim edits.

STATUS was CHECKPOINT before this entry; set to RESUMING because there is verified-incomplete, actively-tracked work to pick back up (this note), not a clean stopping point.
_OLD_PAUSED_AT_BELOW_ (superseded by the entry above, kept for the A12/frontend-strategy-revamp context it still carries):        A12 is Track A's last DOC4 build step; all frontend follow-on work (demo-polish, sim-alignment, and this session's frontend-strategy-revamp — see [A12-frontend-strategy-revamp] Learnings entry) is Systems-lead/human-directed follow-on, not a new numbered step. Nothing is blocking the agent side. This session specifically: implemented the Frontend Strategy doc's §10.1/§10.2 tiers (verified green, not committed); §10.3 (unit lat/lon on best_unit; evaluation-runs router) and §10.4 (IA restructuring, fund-flow timeline, zoom-bound map) are deliberately deferred, the former needing Track B/Algo-dev coordination, the latter gated on it by the doc's own sequencing. Also still waiting on the Systems lead for the carried-over items: (a) confirm A12's Done When evidence is acceptable to close the step; (b) Sync 8 stays open until Track R's R3 lands and the Systems lead + Designer check Section63Draft's field labels against it; (c) the `graph.account_ids` addition (a Track B/Algo-dev-owned file) is a heads-up for the Algo dev; (d) a go/no-go + NVIDIA NIM key decision on Workstream 6 (LLM alert explanations), still unbuilt; (e) the RJ-vs-JH four-demo-state decision raised by the data/ pull, and whether/when to wire world-sim's registry.py to the new real-data CSVs. Separately, `apps/api/tests/integration/test_recovery.py`'s kill-9/outbox-resume assertion failed once during an earlier session's `npm run ci` runs and passed on every other run — confirmed NOT caused by A12; flagged as a pre-existing, load-sensitive flaky test on this machine, not re-investigated further here.
NEXT SYNC POINT:  SYNC 8 (Track R Step R3 -> Section63Draft field labels; Systems lead + Designer). Still open: SYNC 5 confirmation (Systems lead + Priyam), SYNC 4 (no OpenAPI -> schema.d.ts step), SYNC 8 (as above; also gates final analytics per the prior note).

[Post-A12 verification session] — Visual verification of the frontend-strategy-revamp IA (Triage/Deployment/Investigate/System Integrity + Command dashboard) against a live local stack (API on :8001, Vite on :5174, world-sim live on :8101), via Playwright/Chromium screenshots logged in as the i4c_analyst demo user. All five destinations render correctly against real (seeded, not mocked) data: Command dashboard KPIs, Alert Operations Inbox, GIS heatmap (real markers/heat cells over base tiles), Bundled Cases table, and the Evaluation Harness (donut + metrics table + cold-start chart).

Two things were INITIALLY flagged as world-sim/Track-B bugs in this log and turned out, on closer measurement, to be correct-as-designed: (a) `worldsim live --speed 60` really does advance sim time at 60 sim-seconds per real second — the first read was a units error (`ClockState.now_sim`/the status endpoint's `sim_time` field is FRACTIONAL DAYS, not hours; 0.22 looked like "0.22 hours, barely moving" but is actually 5.3 sim-hours, correct for the elapsed wall time). (b) The apparent "0 complaints forever" in the `free` scenario is `config/sim.default.yaml`'s own calibrated `lag: {min: 0.5h, median: 6h, max: 48h}` reporting-delay model (Appendix A) doing exactly what it's supposed to: a generated complaint doesn't become an *observed* (reportable) one until its sampled lag elapses, confirmed by direct reproduction of `World.step()` + `observe()` outside the live loop (68 complaints generated over ~0.093 sim-days, earliest `observed_at` 0.233 days out — nothing wrong, just not yet due). Correcting the record here since an earlier version of this note called (a) a bug; no world-sim code was touched.

One REAL bug found and fixed (in Track A's own ownership — apps/api, not world-sim): the SSE `sim.time` event that apps/web/src/shared/stream/useStream.tsx listens for to drive the on-screen sim clock was never published by anything. `alerting/interfaces/stream.py`'s `_heartbeat_loop` had a comment claiming "heartbeat is a sim.time event" but actually published `SseEvent(name=":heartbeat", data={})` — an unrelated, dataless keepalive the frontend explicitly no-ops on. No code anywhere emitted an event named `"sim.time"`, so `useSimTime()` stayed `null` forever and the UI showed "SIM --:--" permanently, regardless of any real SSE-reconnect timing (which was in fact fine once cookies settled — a red herring from testing too early). Fixed by publishing `SseEvent(name="sim.time", data={"sim_time": response.sim_time.isoformat()})` from `intake/interfaces/routers.py`'s `POST /ingest/tick` (via `request.app.state.sse_hub`, the same hub analytics already uses for `heat.version`, following that existing passthrough pattern — no new port/callback plumbing needed since the router already has `request.app.state` and `IngestResponse.sim_time` already carried the freshly-advanced clock value). Required a new facade-only import, `intake` -> `nakabandi.alerting` for `SseEvent`; checked for cycles first (alerting imports only access/audit/shared, not intake) and confirmed with `lint-imports` (19/19 contracts kept). Stale docstrings in both files corrected to match. Verified: `ruff check`/`format`, pyright (0 errors), full `pytest apps/api/tests` (577/577 passed), and a live Playwright check showing the SIM clock go from stuck "--:--" to advancing in real time (00:34 -> 00:52 over 18s at speed=60).

[Post-A12 verification session, cont'd] — User caught two more issues live in the browser after the sim.time fix: Alert Detail 500ing, and both seed alerts showing exactly 100% confidence. (1) Alert Detail 500: `no such column: intercept_assessments.best_unit_lat` — the uncommitted interception diff (domain/units.py, domain/types.py, infrastructure/repositories.py, application/use_cases.py, alerting/interfaces/alerts.py; ~24 lines, self-consistent) already adds `best_unit_lat`/`best_unit_lon` to the ORM model, but the on-disk dev `nakabandi.db` predates it — `create_all()` never alters existing tables (A8's learning, rediscovered). Not a code bug; fixed by running `scripts/reset.py` (API stopped first, as its docstring requires) to drop/recreate all tables and reseed through the real intake use cases (13 accepted, 0 rejected). Verified: GET /alerts/{id} 200 with a full body, and a live Playwright screenshot of the Alert Detail drawer rendering correctly (confidence bar, timing chart, interception assessment, evidence). (2) Confidence exactly 1.0 on both alerts is NOT a bug: `model_versions.scorer` in the forecast payload reads `"fallback"` — `forecast/application/use_cases.py`'s `GenerateForecast.__init__` tries `model_store.load_scorer()` for the trained v1 `HistGradientBoostingScorer` (B6) and falls back to the v0 `HeuristicScorer` with a boot warning ("model files missing, using heuristic_v0. Run TrainModels to build the v1 model") whenever no trained model file has been persisted — true of this sandbox, since nothing has ever run `TrainModels`. With the hand-written seed fixture's 2-location toy geography, the heuristic scorer has exactly one plausible candidate per complaint at every resolution (location/cell/district), so it legitimately outputs prob=1.0 rather than a spread — confirmed real computation (per-complaint timing curve, evidence codes, novelty all vary per alert), not a hardcoded stub. This is the same cold-start behavior the System Integrity page's "Cold-Start Hit Rate by Complaints Ingested" chart documents (near-zero at low complaint counts, climbing toward the trained baseline by ~500). Training a real v1 model needs `TrainModels` run against enough accumulated (complaint, outcome) pairs — not attempted here; flagged as a possible next step if a less-degenerate demo confidence spread is wanted, but out of this session's scope and likely Track B's call on when/how to calibrate it.

Local dev stack notes for next session: API needs `API_SERVICE_KEY` + `JWT_SECRET` env vars (no defaults, no .env file present in this sandbox); world-sim needs `--api-url http://127.0.0.1:8001/api/v1` (the `/api/v1` prefix is not automatic) and `WORLDSIM_SERVICE_KEY` matching the API's key; world-sim's `--db` defaults to a relative `world.db` and will throw `unable to open database file` if the process's cwd can't create it there — pass an absolute `--db` path; `SimClock.advance_to` is monotonic and silently ignores an earlier timestamp (A3's learning, rediscovered here), so restarting world-sim against an already-advanced dev `nakabandi.db` needs `WORLDSIM_EPOCH` set past the DB's last recorded tick or the clock visibly stalls even though ticks keep POSTing 200 OK.

STATUS is one of ACTIVE | RESUMING | BLOCKED | CHECKPOINT. Only this track's owner (and its agent) edits this file.
Update it after every completed step and before ending a session.

## Learnings
One line per entry, newest last: [Step] — what was found (a gotcha, a rejected approach and why, a decision not in the docs).
[A1] — Toolchain: uv 0.12.17 (brew), Python 3.11.16 via `uv python install`, gitleaks 8.30.1 (brew), Node 24.6.0 (Node 20 is end-of-life; the Integration Owner pinned "Node 24 LTS" in the docs, commit f56f1ac). pre-commit is a workspace dev dependency in the root pyproject (pinned by uv.lock, installed by `uv sync`, then `uv run pre-commit install`), not a `uv tool`, so all machines get the same version.
[A1] — import-linter can only resolve forbidden external packages that are installed, so sqlalchemy, fastapi, httpx and requests sit in the root `dev` dependency group purely for the domain-purity contract. Runtime dependencies are added per step. Facade-only rule = one forbidden contract per module with `nakabandi.<other>.**` and `allow_indirect_imports = True`; layers contract marks `interfaces` and `infrastructure` optional because not every module has them. All 17 contracts were shown to fire on deliberate violations; a facade import correctly passes.
[A1] — NOT encoded: the DOC 2 §2.6 row "only evaluation may import the oracle client". `nakabandi.evaluation.oracle_client` does not exist yet (Track B, B7), and a forbidden contract needs the module to exist. Until then the facade rule already blocks every non-evaluation module from reaching it, and `main.py` may not import evaluation. Add an explicit contract when B7 creates the file. Also not encodable: "only pipeline and main.py import several facades in sequence"; only "no module imports pipeline" is enforced.
[A1] — eslint-plugin-boundaries 7.2 uses the `boundaries/dependencies` rule with `policies` (not the old `element-types`). It silently skips extensionless TypeScript imports unless `settings["import/resolver"] = { node: { extensions: [".ts", ".tsx"] } }` is set; without it the cross-feature rule reported nothing at all. No extra resolver package was needed (eslint-import-resolver-node ships with the plugin). Always test a boundary rule with a deliberate violation before trusting a green lint.
[A1] — Root scripts `dev`, `types`, `seed`, `reset`, `sweep` are placeholders (`scripts/not-yet.mjs`, exit 0 with a message) until their steps land. `.env.example` lives in infra/ (DOC 2 layout) and lists only the four bank-sim names DOC 3 defines; API Settings names are added at A2.
[A1] — Shell gotcha: the dev shell is zsh, which does not word-split unquoted variables, so `for m in $list` loops and `uv run "cmd args"` silently misbehave. Use literal lists or arrays.
[A2] — LC-7 names every policy.yaml key but gives a literal starting value for only a few (forecast.horizons_min [30,60,120]; forecast.candidates.max 200; forecast.novelty_threshold 0.7; interception.thresholds.interceptable/marginal 0.7/0.3; interception.targets 3; alerting.delivery.max_attempts 5 and backoff_s 5, from "5 s, 10 s, ... capped 5 min"). Every other numeric leaf (radius_km, abstain thresholds, speed_kmh, severity_bands, budget_per_shift, k_threshold, amount/confidence bands, label_rate, ...) has no DOC3 source and is a Track-A-authored starting value, the same way Appendix A treats simulator priors: a seed for Track B to calibrate and sweep, not a fact. config/policy.yaml's header comment marks which is which; do not read an unmarked value as calibrated.
[A2] — Policy.load() aborting on both an unknown key and a missing one (pydantic `extra="forbid"` plus no field defaults) means it also aborts on a key DOC3 names but a value-writer forgot to fill in — that's deliberate: a silently-defaulted policy key is worse than a boot failure naming the exact path.
[A2] — Two locked contracts (LC-1, LC-2) live in packages/contracts, which the import-linter contract "contracts-are-leaf" forbids from importing `nakabandi`. So SimTime's tz-aware-UTC rule (LC-2) is implemented twice, once in `nakabandi_contracts.ingest` and once in `nakabandi.shared.types`, deliberately not shared as one symbol — this is a boundary consequence, not duplication to clean up later.
[A2] — Extended `.importlinter` with a "shared" facade contract (other modules may import `nakabandi.shared` but not `nakabandi.shared.<file>`), mirroring the existing per-module facade contracts. DOC2 §2.6's facade rule names "another module" generically and shared is explicitly "the only code every track imports" (DOC3 Shared Kernel), so the same protection was extended to it; A1 had not, since shared had no content yet. Flagged for the Integration Owner because it's a boundary-config change, not just new files under the step's named folder targets.
[A2] — `npm run types` now runs scripts/gen_contract_schemas.py then `git diff --stat --exit-code` on the schemas folder; against untracked files git diff is a no-op (always exits 0), so the check only does real work once the schemas are committed/staged. Verified both directions with the files staged: changing the ingest.py model without regenerating makes `npm run types` exit 1 with a diff shown; regenerating brings it back to exit 0. When checking a "no diff" claim like this, confirm the files are tracked/staged first, or the check is vacuous.
[A2] — `git diff --stat` for this step is wider than the step's four named folder targets: it also touches package.json (types script), root pyproject.toml (testpaths), .importlinter (new shared facade contract), apps/api/pyproject.toml and packages/contracts/pyproject.toml (new runtime deps), and uv.lock. Each is a direct, necessary consequence of building shared/contracts (nothing outside Track A's own root tooling), but it is wider than "targets above" reads literally — flagged for the Integration Owner rather than assumed acceptable.
[A3] — LC-1's `RegistryUpdate.banks/regions/cells` are still opaque `list[dict]` (unchanged): rather than widen the locked contract to add typed shapes (a Sync-Point-reopening change per the Contract Change Process, right after Sync 1 was just confirmed), geo parses those dicts internally against DOC 2 §2.3's entity fields (geo/domain/parsing.py) and never promotes the shape into packages/contracts. Track A stays free to swap the parsing without touching LC-1 or re-notifying other tracks.
[A3] — No DOC names the /ingest/* service-key header or its env var. Header: `X-Nakabandi-Service-Key` (matches the `X-Nakabandi-*` naming DOC 3 LC-6 already uses for the bank webhook). Env var: bare `API_SERVICE_KEY` (no `NAKABANDI_` prefix), because DOC 4 §4.4's deployment checklist names that exact variable and the SAME value already authenticates apps/bank-sim's calls (DOC 3 Bank Gateway Simulator config.ts) — one secret, read by both processes. `shared/config.py`'s `Settings` picks it up via `Field(validation_alias="API_SERVICE_KEY")`, bypassing the `NAKABANDI_` prefix applied to fields DOC 4 §4.4 doesn't name. Also added `database_url` (bare `DATABASE_URL`, same reasoning, defaults to `sqlite:///nakabandi.db`).
[A3] — Seeding `SimClock`'s boot value from wall-clock `now()` (via `SystemClock`) is a trap: `advance_to` is monotonic and silently ignores a target earlier than current, so any demo/seed data dated before the moment the process happens to boot would freeze the clock forever with no error. Fixed by seeding from a fixed Unix-epoch constant instead (`shared.SIM_CLOCK_EPOCH`, in `shared/clock.py`, exported through the facade) so the first real batch always advances it forward. Caught by actually running `npm run seed` end-to-end, not just by the pytest suite (which never asserted on `response.sim_time`) — a green test suite did not catch this; running the real CLI path did.
[A3] — Two ingest artifacts, deliberately not one: `apps/api/tests/fixtures/mini_ingest.json` (the step's named "20-row hand fixture", a single JSON object with one key per batch type, used by pytest for precise per-endpoint assertions) and `data/seed/mini_ingest.jsonl` (true line-delimited JSONL, one `{"kind", "payload"}` object per line, consumed by `scripts/seed.py` — DOC 4 says "JSONL file" literally for the seed CLI). Both are stand-ins for Step B1's golden stream (STUB/MOCK STRATEGY: swap at Sync 2) and carry the same underlying scenario data, hand-written twice rather than generating one from the other.
[A3] — `nakabandi.shared`'s facade grew three more exports beyond what the step's literal `shared/infrastructure/uow.py` target implies: `Base` (the declarative base every module's ORM models attach to, needed so intake/geo infrastructure can reach it without violating the facade-only rule), `SqlAlchemyUnitOfWork` (routers build one per request; import-linter's facade-shared contract caught the direct `shared.infrastructure.uow` import on the first `npm run ci` and this was the fix), and `SIM_CLOCK_EPOCH`. Also added ruff `ignore = ["B008"]` to the root pyproject (FastAPI's `Depends()`-in-default-argument pattern, which every router from here on will use, otherwise trips bugbear's mutable-default-argument rule) — another root-tooling change beyond the step's named folders, same pattern as A2's note above; flagged for the Integration Owner.
[A3] — Correction to an over-cautious read: initially treated SYNC 2 as blocking A4 (Track B is still on B1) and stopped at CHECKPOINT. Re-reading DOC4 §4.1a, SYNC 2's own text names exactly what it gates — "before Track B Steps B8/B9 rely on live ingest" — which is Track B's problem, not Track A's; nothing in DOC4 makes A4 wait on it. Checkpoint 3 (§4.3) is the next gate that actually involves A3, and it needs A6 and A7 too (and Track B B2-B4), so it's well past A4. Lesson: "a Sync/Checkpoint follows this step" (the ACTIVE→CHECKPOINT trigger) means checking what that Sync/Checkpoint's own text says it gates, not just that the step's name appears in its "After ..." clause.
[A4] — Found via the audit hash-chain test, but a systemic issue, not an audit-only one: SQLite's driver silently returns a NAIVE datetime from a `DateTime(timezone=True)` column on read, even though a tz-aware value was written. `/audit/verify` was failing on an UN-tampered chain because the hash was computed once at append time (tz-aware `at`) and again at verify time on a freshly re-read row (naive `at`, no "+00:00" in the isoformat string) — two different canonical strings, two different hashes, no tampering involved. Fixed at the root with a `UTCDateTime` TypeDecorator in shared/infrastructure/db.py (re-attaches UTC tzinfo on read, exported via the shared facade) and migrated every existing DateTime column (intake's five tables, audit's one) to it, not just audit's. Any future module's timestamp column should use `shared.UTCDateTime`, never a bare `DateTime(timezone=True)`, or the same class of bug recurs silently.
[A4] — A second, related clock bug, inverse of A3's SIM_CLOCK_EPOCH one: `Login` originally computed the session cookie's expiry from the injected (simulated) `Clock`, which starts at SIM_CLOCK_EPOCH (1970) and had not been advanced in a fresh test app — so every login cookie was already "expired" in 1970 and httpx's cookie jar silently dropped it (login returned 200, but no session cookie ever reached the client; every subsequent `/auth/me` call then got 401 for what looked like no reason). Cookie/JWT expiry is a wall-clock, transport-level concern like LC-6's webhook timestamp window (DOC 2 §2.4), never the simulated timeline, so `Login.run` now calls `SystemClock().now()` directly for `expires_at` and no longer takes a `clock` parameter at all (nothing else in Login needed it). Same lesson as A3's SIM_CLOCK_EPOCH fix in the opposite direction: name, for every new timestamp, which clock (sim or wall) it should come from before wiring it, rather than defaulting to whichever Clock instance is already in scope.
[A4] — DOC 3 M5 states two numeric constants directly in prose rather than as an LC-7 policy.<x> key: the login rate limit ("5 per minute per IP") and the session TTL is not stated at all (only that the JWT carries an `exp` claim). LC-7 is locked and has no access.session_ttl or access.login_rate_limit key. Kept both as named module-level constants instead of inventing a policy key unilaterally (`access/interfaces/rate_limit.py: MAX_ATTEMPTS_PER_WINDOW=5, WINDOW=1min`; `access/application/use_cases.py: SESSION_TTL=12h`), flagged here for the Integration Owner in case either should become a Contract Change to LC-7.
[A4] — DOC 2's User entity field list (§2.3) has no `username` field, but DOC 3 M5 names `Login.run(username, password)`. Rather than add a column DOC 2 does not name, login matches against the existing `name` field (access/domain/entities.py); the wire-facing LoginRequest body still uses `username` as the JSON key for continuity with DOC 3's wording, it just maps onto `name` internally.
[A4] — Demo user scopes (access/application/demo_users.py) use placeholder ids ("demo-state-1", "demo-district-1", "demo-bank-1") since no real geo/bank registry data exists yet at this step (that lands via Track D's D5 geo curation and whatever registry data Track B/world-sim seeds). These will not resolve to real rows until Track D's curated data (or a real registry) is loaded; harmless for A4 (nothing yet reads geo tables through a demo user's scope) but flagged so a later step does not assume these ids are real.
[A4] — argon2-cffi (password hashing) and PyJWT (session tokens) are new runtime deps not named in DOC 2 §2.2's stack list by package name — but the TECHNIQUES are: "passwords hashed with argon2" and "Login issues a signed JWT". Checked against the AP-07/PP-09 stack-substitution trigger before adding either: this is picking the standard implementation library for an already-named technique, not substituting a different technique, so it proceeded without asking. Documented here per AP-07's guard ("name the exact library ... for every stack decision") since DOC 4's Context Snapshot does not.
[A4] — No DOC names the session cookie's name; chosen `nakabandi_session` (access/interfaces/dependencies.py), documented as the one place it is named. `/auth/demo-users` also ships without the "hosted demo mode only" gate DOC 3 M5 describes — that flag is explicitly a Step A11 target ("/auth/demo-users flag", DOC 4 §4.4-adjacent A11 entry), so building it unconditionally now and letting A11 add the gate avoids inventing an undefined demo-mode config field ahead of the step that owns it (AP-08).
[A4] — `nakabandi.shared.messages` had no entries for any of A3's or A4's custom error codes (COMPLAINT_*, GEO_*, HOP_UNKNOWN_COMPLAINT, ACCOUNT_UNKNOWN, SERVICE_KEY_INVALID, LOGIN_FAILED, SESSION_MISSING/INVALID, TOKEN_INVALID, FORBIDDEN_PERMISSION/SCOPE, ROLE_MISMATCH) — every one of them was silently falling back to the generic FALLBACK_MESSAGE in any error JSON response, caught only by eyeballing the curl transcript's 403 body. Backfilled all of them in this step; the messages.py docstring says modules add their own codes as they are built, so from here on add the message in the SAME change that introduces a new DomainError code, not after.
[A5] — uvicorn is a runtime dep added at this step (not A4), consistent with the A4 learning note ("TestClient against the same ASGI app — uvicorn is not wired until A5"). Added to apps/api/pyproject.toml here.
[A5] — STATIC_DIR env var is unset outside the Docker image; main.py guards the SPA mount with `if settings.static_dir is not None and settings.static_dir.is_dir()` so local dev and tests never try to mount a non-existent path. Tested explicitly by test_static_dir_is_not_mounted_when_unset.
[A6] — Pipeline port Protocols (_ComplaintRepo, _ClusterService, _Forecaster, _Interceptor, _AlertService) are defined inside pipeline/process_complaint.py, not imported from the modules they abstract. This avoids cross-module imports while giving Pyright something to check attribute accesses against. Stubs in tests/stubs/ implement these protocols structurally — no explicit `implements` marker needed.
[A6] — Complaint entity required a `failed_stage: str | None` column (added to intake entity, ComplaintModel, and SqlComplaintRepo) to record which pipeline stage caused an unprocessed status. This is an intake table implementation detail, not a LC change, so no Contract Change Process was needed.
[A6] — Golden test imports from `stubs.pipeline_stubs` (not `tests.stubs...`) because pytest's testpaths root is `apps/api/tests`, making `stubs` the top-level package visible to imports.
[A6/MP3] — Sync 3 / MP3 confirmed: merged origin/feat/track-b (B1-B4), resolved geo/__init__.py interface merge cleanly; swapped pipeline stubs to use real Track B domain types and logic (B2 ClusterIndex, B3 Interception ladder/verdict/assessment, B4 Forecast/LevelForecast/TimingForecast); npm run ci 297/297 passed, 18/18 contracts kept.[A7] — Scheduler (shared/scheduler.py) stores lambdas; those lambdas capture SqlAlertRepo bound to the session that was open when EscalateAlert/ExpireAlert was scheduled. In tests, always fire scheduler.run_due() inside the SAME UoW that registered the timer, otherwise the captured session is closed and the lambda errors silently. In production this is fine because the FastAPI lifespan scheduler task opens a new session per tick — but tests must not cross UoW boundaries with timer lambdas.
[A7] — SSE endpoint (GET /api/v1/stream) is an infinite async generator; Starlette TestClient's client.stream() blocks synchronously waiting for the generator to close, which never happens. Do not use client.stream() in synchronous pytest — the test hangs. Instead, test SseHub logic directly (subscribe/push/sentinel) with asyncio.run() in pure unit tests, and smoke-test the HTTP endpoint with a unauthenticated/authenticated GET confirming 401/200 without consuming the stream.
[A7] — import-linter facade violation: alerting imported Principal from nakabandi.access.domain.principal directly instead of through the nakabandi.access facade. Always check the owning module's __init__.py __all__ first; if the symbol is listed there, import from the facade. Principal, authorize, get_principal and Scope are all in nakabandi.access.__all__.
[A8] — Alert -> complaint linkage did not exist in the DOCs (an Alert stored only forecast_id and cluster_ref, and alerting may not read intake tables, LC-10). Resolved at the follow-up commit by giving the Alert a `complaint_id` anchor (the complaint of the forecast that raised it; a later merge keeps the original anchor, like created_at) and scope columns `scope_state_id/scope_district_id/scope_bank_id` copied from the target location at raise time. NOT in DOC 2 §2.3's Alert field list beyond scope_district_id/scope_bank_id (state added because access.authorize compares a state-scoped principal against the resource's state, and complaint_id is the anchor DOC 3 M6 requires of every lien): a DOC 2/3 update for the Integration Owner. Ports: `LienContextPort` (intake.LienContextLookup: for_account(complaint_id, account_id), complaint_ref, traced_accounts) and `TargetScopePort` (geo.LocationScopeLookup), both wired in main.py via app.state factories. A hold on an account traced from a DIFFERENT complaint than the alert's is now rejected (LIEN_INVALID). Local dev DBs created before this need `npm run reset` (create_all does not add columns).
[A8] — Local status enums, not LC-2 shared enums: ActionStatus (pending | recorded | applied | rejected | released), DeliveryStatus (pending | sent | failed | dead), DeliveryChannel (sse | email | sms | webhook), WebhookKind (alert_notice | hold_request) live in alerting/domain. DOC 2 §2.3 lists the delivery values; the action values are the LC-6 callback statuses plus `pending` (request sent, bank not yet answered) and `recorded` (internal action, nothing to wait for). LC-6's request_id IS the action id; the delivery idempotency key is `hold_request:<action_id>`.
[A8] — Which clock, per timestamp (the A3/A4 lesson): Delivery.next_attempt_at, created_at and sent_at and the webhook X-Nakabandi-Timestamp are WALL-clock (transport); Action.at/status_at, timeline entries and every *_sim field in the LC-6 body are SIM time. `backoff_seconds` caps at 300 s as a named constant (DOC 3 gives the cap only in prose, LC-7 has no key for it; same treatment as A4's constants); the 5 s base and 5 attempts come from policy.alerting.delivery.
[A8] — A denial must be audited AND survive: SqlAlchemyUnitOfWork rolls back on any exception, so RecordAction appends the `action.denied` audit entry and re-raises Forbidden, and the router commits before re-raising (`except Forbidden: uow.commit(); raise`). Any future use case that must audit a refusal needs the same two halves.
[A8] — Both directions of the hold_request guarantee are enforced: the Delivery entity raises if a hold_request has no action_id, and the deliveries table carries a CHECK constraint (ck_deliveries_hold_request_has_action); a test bypasses the entity to prove the database refuses it. EnqueueDeliveries.enqueue_hold_request takes the Action itself, so no caller can build one from loose values. rendered_body always carries the masked ref (`****` + last 4) while the wire payload to the bank carries the full ref (LC-6 masks only alert_notice); the outbox view never returns the payload.
[A8] — Settings/env added: WEBHOOK_SECRET (bare, already in infra/.env.example, same secret bank-sim verifies with), NAKABANDI_BANK_WEBHOOK_URL (full URL of POST /webhooks/nakabandi), NAKABANDI_SMTP_HOST/SMTP_PORT, NAKABANDI_SMS_PROVIDER_URL/SMS_PROVIDER_KEY, NAKABANDI_OUTBOX_WORKER_ENABLED (default true; tests/conftest.py sets it false and drives `app.state.run_outbox_once` or AlertService.deliver_outbox with an explicit wall time). infra/.env.example was NOT touched (Integration Owner file): it should list the NAKABANDI_* names. Unconfigured webhook = the delivery fails and dead-letters visibly rather than being sent unsigned; unconfigured SMTP = recorded-only (provider=outbox); unconfigured or failing SMS provider = automatic OutboxSms fallback. ProviderSms is a plain httpx POST of {to, body} with a bearer key because no DOC names an SMS vendor; pick the real sandbox when one is chosen.
[A8] — Follow-up, items first flagged as deferred and then done: (a) ActionRecorded: the LC-3 class already existed in shared.events but was not exported; now exported, main.py owns one EventBus (app.state.event_bus), RecordAction publishes it after the action and a failing subscriber is logged, never undoes the human's action. (b) alert_notice: RaiseOrMergeAlert calls an on_created hook; AlertService queues one masked notice per traced account (recipient bank:<bank_id>) in the alert's own unit of work, only on creation, not on merge; skipped when no intake lookup is wired. (c) AlertDetail now has deliveries, actions and allowed_actions; forecast, interception and case_id are still absent (other modules' data). (d) ReviewLien: DOC 3 names the timer but not its effect; implemented as the minimal safe reading, a `alert.hold.review_due` timeline line at the hold's review_at (stored in action params), once, only for pending/applied holds, no status change or delivery; the Integration Owner should confirm or extend. ReconcileOutcome is NOT done: DOC 4 assigns it to A10 (with budget and feedback). (e) Scope: enforced for list, detail, acknowledge, RecordAction (scope denial is audited like a permission denial), the outbox view and the SSE stream (LC-5, events carry the alert's scope). An alert whose scope could not be resolved is visible only to unrestricted principals (fail closed).
[A8] — Test gotchas: intake normalises account refs to UPPERCASE (fixture "acc-5" is stored as "ACC-5", found only after a NoResultFound); the fixture's hop c1 ACC-1 -> ACC-5 is 480000 paise, which the tests use as the disputed amount. Test modules import siblings as `from alerting.test_use_cases import ...` (testpaths root, per the A6 note). `datetime.now` and `time.time` are banned by ruff even in tests: read wall time through SystemClock.
[A8] — CONSEQUENCE of scope enforcement to fix upstream: the demo users' scopes (access/application/demo_users.py) are A4 placeholders (demo-state-1, demo-district-1, demo-bank-1) that match no real region or bank id, so once real registry data is loaded a scoped demo user (state investigator, district officer, bank nodal) sees NO alerts and cannot act on any. Track D's demo_users_seed.csv uses UP / UP-LKO / BNK-001. They must be aligned with the ids the registry actually carries (Track D D5 data / Track B world-sim) before the demo; left unchanged here because the real ids are not decided. The unrestricted roles (i4c_analyst, admin) are unaffected. Tests use the placeholder ids via a fixed scope lookup and check the real geo lookup separately.
[A8] — Process note: a `git checkout <file>` used to undo a deliberate mutation test also reverted that file's uncommitted work; it had to be rewritten. For mutation checks copy the file aside and restore the copy.
[A9] — Design, in the order the step's prompt hint forces it. (1) Read models come from events only, but LC-3 events carry IDs only, so the projectors look facts up through analytics-owned ports (`ProjectionSource`, `GeoCatalogPort`) implemented in the new `nakabandi/wiring.py` over the alerting, forecast, intake and geo facades. (2) Nothing published AlertRaised or ForecastGenerated: AlertService.raise_or_merge now publishes AlertRaised for each NEW alert (AlertResult gained `created_ids`), and ProcessComplaint gained an optional `bus` and publishes ForecastGenerated after the chain (also for a stale forecast; never after a failed stage). (3) Projector writes must ride the publisher's session, or SQLite deadlocks a second writer against the publisher's uncommitted transaction: main.py builds a fresh EventBus per unit of work (`app.state.event_bus_factory(session)`), applying `app.state.bus_registrars` (analytics is the first; tests append their own). This replaced the app-wide `app.state.event_bus` from the A8 follow-up. (4) heat.version is published from a SQLAlchemy `after_commit` hook, so a client refetching on it always sees the rows, and a rolled-back unit announces nothing.
[A9] — Rollup semantics chosen where DOC 3 is silent. Live layer = one row per alert at its target LOCATION only (mass = that location's item probability x timing.p120 from the alert's anchor complaint's latest forecast, count 1); cells and districts are summed from locations at query time via the geo catalog, so two alerts sharing a cell never double-count the cell's mass. Potential layer = every forecast's cell AND location rows (mass = prob x p120), alert_count = 1 per contributing forecast, decayed at query time by policy heatmap.decay_half_life_hours over potential_lookback_hours; above location level it reads only its cell rows (a forecast's cell and location levels describe the same mass). k-threshold therefore counts contributing forecasts for the potential layer. district lat/lon = mean of its cells' centroids. `from` is ignored for the potential layer (its window is `to` minus the lookback).
[A9] — DOC 3 M3 deviations for the Integration Owner: (a) AlertUpdated is NOT projected: a merge would add mass again and an expiry would need to remove it, and neither can be exact without a per-alert ledger table, which LC-10 does not list; live alerts age out by hour bucket instead. (b) The `cluster_id` (LEA roles) filter is not offered: a rollup key has no cluster. (c) No endpoint for filter options (categories, amount/confidence bands from policy): DOC 3 says they come "from policy via the API" but names no endpoint and LC-4 is locked, so the web app cannot fetch them yet. (d) /analytics/timeseries and /analytics/live-metrics shapes are not in DOC 3/LC-4: defined here as {layer, from_, to, version, points[{hour, value, alert_count}]} and {generated_at, version, window_hours, alert_count, expected_mass, active_locations}. (e) No bundled GeoJSON loader and no unit markers: data/geo holds no boundary files yet (Track D). (f) A bank-scoped principal may view only level=location (a cell or district mixes in other banks' locations) and gets 403 otherwise.
[A9] — Two Track B defects worked around in Track A's wiring, for Track B to fix at source: forecast's tables sit on their own `sa.MetaData()` (forecast/infrastructure/repositories.py), so `create_all` never created them and nothing outside a test would have had a `forecasts` table: main.py now also calls that metadata's create_all at boot; and those tables use a plain `DateTime(timezone=True)`, which SQLite returns NAIVE (the A4 gotcha), so wiring.py re-attaches UTC to `generated_at`. Using `shared.Base` and `shared.UTCDateTime` in forecast would remove both workarounds.
[A9] — Layering lesson, caught by import-linter on the first run: the application layer may not import its own module's infrastructure, so value objects and the repo Protocol (`RollupDelta`, `BucketSum`, `RollupRepo`) live in analytics/application/ports.py and the SQL repo imports them, not the reverse. Also: the demo users' placeholder scopes (A8 note) mean scoped demo users see nothing on real registry ids; the heatmap tests register regions named demo-state-1 / demo-district-1 / demo-bank-1 to exercise scoped roles.
[A10] — A REAL BUG FOUND WHILE BUILDING THIS: a real Forecast has no `amount_paise` (the amount belongs to the complaint), so RaiseOrMergeAlert read 0 and severity was ALWAYS LOW in production; only the test fakes (which carried an amount) hid it. The amount is now looked up through intake (`LienContextLookup.complaint_summary`, via AlertService's lien_context); a forecast that does carry `amount_paise` still wins. The same amount feeds budget priority. Separately, policy.alerting.severity_bands are so low that almost any real complaint scores CRITICAL (log1p(500000) x 0.75 x 0.3 is about 2.9 against a critical band of 0.8): Track-A-authored placeholders that need calibrating against the log1p scale before severity means anything.
[A10] — Budget. Priority = confidence x log1p(amount) x interception_probability (the assessment's own; the policy floor if a caller's assessment has none), stored on the Alert with its `budget_rank`. The queue is (district, shift): the district officers' queue, the first recipient of every alert (routing is still a stub). DOC 3 speaks of (role, jurisdiction) queues, but an alert has ONE is_deferred flag, so one queue defines it; state and I4C see the same flag. Each raise re-ranks the whole queue for that shift, so a later, higher-priority alert can push an older one into the backlog. Exploration is seeded by the alert id (md5 of "seed:id"), independent of input order. GET /alerts gained `view`: queue (default: not deferred), backlog (deferred only), all, and review (the uncertainty-ordered feedback queue); DOC 3 names no endpoint for either tab, so these are query values on the existing endpoint rather than new routes. Deferred alerts still feed the heatmap (analytics projects every AlertRaised).
[A10] — Reconcile. Hit = cash-out at the alert's target location with event_at in [window_start, window_end] (closed at both ends); late = after window_end up to and including grace; miss = the TIMER at window_end + grace with no reconciled outcome, never data arriving. One reconciled outcome per alert; an officer's outcome is a separate row that may disagree and both show in AlertDetail.outcomes. A lagged report that arrives after the miss was called does NOT overturn it (one automatic outcome per alert); that loses some lagged truth and is worth revisiting if the evaluation shows misses that were really late hits. Wiring: intake now publishes ObservationIngested after each observation batch (IngestService/IngestObservations take an optional bus; the ingest router passes the per-unit-of-work bus), and main.py registers ReconcileOutcome on it. The tick's RebuildTimers now registers a miss timer for EVERY alert without a reconciled outcome, whatever its status, so an expired alert still gets its miss.
[A10] — MarkOutcome. Needs MARK_OUTCOME and the alert in scope (both audited on success; a denial is a plain 403). A `location_id` is allowed only with hit or late and must be a registry location (422 otherwise, checked through the geo scope lookup). A repeat of the same (alert, result, location) returns the existing row (200) and does nothing more. It closes an alert that was acknowledged or actioned; open, escalated and expired alerts are left as they are (feedback on an expired alert is recorded, not reopened). The Outcome shape is LC-4's {id, alert_id, result, source, at}. New columns: alerts.priority, alerts.budget_rank, outcomes.source/actor_id/location_id/reason (dev DBs need `npm run reset`).
[A10] — (SUPERSEDED, see [A11-prep]) STUB for graph.apply_confirmed: it was a logged recording fake at the time; it is now a real adapter (`wiring.GraphConfirmedCashOut`) over the graph write path added in [A11-prep].
[A10] — Two small correctness catches by the tests: uncertainty(0.3) and uncertainty(0.7) differ in the last floating-point bit, which silently defeated the 'ties by severity' rule until the ordering key rounded it; and a `str.replace` on a source file that ruff format had already re-wrapped silently did nothing, so after any scripted edit re-check that it applied before trusting a green run.
[A11] — CORRECTION OF AN EARLIER CLAIM, found at the start of A11: 'Sync 3 / MP3 confirmed' and Checkpoint 3 ('a complaint posted through /ingest produces a real alert, visible via GET /alerts') were NOT actually met. Verified by search: nothing in the running app builds a ClusterService, a Forecaster or a ProcessComplaint, or triggers the chain after an ingest; POST /ingest/complaints only stores the complaint. The A6 golden test runs the pipeline against stubs that wrap real Track B DOMAIN functions, which proved the shapes but not the live wiring. Consequences: (1) every alert in a running API today comes from a test or from calling AlertService directly; (2) the A9 projectors and the A10 reconcile/budget code are proven by tests that publish the same events themselves, but would fire in production only once the chain runs; (3) A11's Done When ('stress run: no lost complaints, no duplicate alerts', p95 stage latencies on the Ops page) has nothing to measure until it does. Real wiring needs adapters between the real facades and the pipeline's ports, which do NOT match today: pipeline._ClusterService.context_for(complaint_id, cluster_id, as_of) vs graph.ClusterService.context_for(cluster_id, as_of); pipeline._Forecaster.generate(ctx, as_of) vs Forecaster.generate(ctx, all_locations, home_district_id, delays_min, as_of); interception.assess takes traced accounts, disputed amounts and active liens the pipeline does not compute; graph's cluster_location_stats (Core table on its own MetaData, like forecast's) is read but never written. That is an integration job spanning both tracks' facades (Algo dev should review the adapter inputs); it should come before the rest of A11, not after.
[A11-prep] — THE LIVE CHAIN IS NOW WIRED (closes the [A11] CORRECTION above; Checkpoint 3 is now genuinely met). POST /ingest/complaints runs, in the same request and unit of work: graph.resolve (every account the complaint reached) -> forecast.generate -> interception.assess -> alerting.raise_or_merge -> analytics projectors. Pieces: `IngestHooks` (intake; on_complaints, on_hops, on_registry; explicit orchestration, not events), `live_pipeline.py` (LiveClusterPort, LiveForecaster, LiveInterceptor, GeoUnitRepo, RegistryCache, and `build_pipeline(session, policy=, bus=, registry_cache=, alert_service=, model_store=)`), `wiring.py` adapters, and main.py factories (`alert_service_factory`, `pipeline_factory`, `ingest_hooks_factory`, `registry_cache`). `build_pipeline` is the injection point Track B's evaluation runner asks for ('the real pipeline is injected from main.py'): call `app.state.pipeline_factory(session, bus)` or `live_pipeline.build_pipeline(...)`. Also added: RetryUnprocessed at boot and every 5 minutes (RETRY_EVERY_TICKS in main.py), a forecast REFRESH when hops arrive later (`ProcessComplaint.run(..., refresh=True)`: the alert merges, ForecastGenerated is NOT re-published, so analytics counts one forecast per complaint), ClusterMerged -> alerting re-key, and AlertDetail now carries `forecast` and `interception` (LC-4), read through `AlertDetailSource`. Proof: apps/api/tests/integration/test_live_chain.py (12 tests, real modules end to end); 10 mutations of it were caught. `npm run ci` green, 481 passed.
[A11-prep] — TRACK B-OWNED FILES I CHANGED (all additive unless marked; for the Algo Dev to review at the merge): graph/application/ports.py (+abstract record_location_observation, move_location_stats); graph/infrastructure/repositories.py (+ their SQL implementations); graph/application/apply_cashouts.py (NEW: ApplyCashOuts, ApplyConfirmedCashOut, CashOutFact); graph/__init__.py (+cluster_of, apply_cashouts, apply_confirmed on ClusterService); graph/application/use_cases.py (CHANGED: ResolveCluster now folds an absorbed cluster's location stats into the survivor); forecast/infrastructure/repositories.py (+SqlForecastRepo.get_by_id). Nothing in interception or the forecast domain was touched. Track A also added scikit-learn to apps/api/pyproject.toml and uv.lock (see below): expect a uv.lock merge conflict if B adds dependencies too; regenerate with `uv lock`.
[A11-prep] — DEFECTS FOUND IN THE B/A SEAMS, and how each was handled. (1) scikit-learn was imported by geo's SpatialIndex, interception's UnitIndex and B6's scorer but declared nowhere: interception failed on every real complaint with ModuleNotFoundError. FIXED: declared. (2) graph's cluster_location_stats had readers and NO writer: nothing ever recorded a cash-out, and apply_confirmed did not exist. FIXED: the graph write path above, fed by ObservationIngested (main.py `register_graph`) and by MarkOutcome. (3) interception's own `units_table` is named 'units' like geo's units table (different columns, `active` vs `status`) and nothing creates it; LC-10 says geo owns units. WORKED AROUND: `live_pipeline.GeoUnitRepo` feeds the interceptor from geo through the registry cache, and main.py creates only interception's intercept_assessments table. Better fix at source: interception's UnitRepo reads through geo's facade. (4) forecast, graph and interception tables sit on their own sa.MetaData(), which create_all never sees: main.py creates them at boot. Better fix at source: put them on shared.Base. (5) forecast's DateTime columns come back NAIVE from SQLite (the A4 gotcha): re-attached in wiring. Better fix at source: shared.UTCDateTime. (6) graph's PointInTimeStats.observation_count counts location ROWS, not observations; LiveClusterPort sums LocationStat.observation_count instead; complaint_count is never populated. (7) graph.resolve publishes ClusterUpdated/ClusterMerged unguarded, so one failing subscriber failed the whole resolve stage: subscribers in main.py are wrapped (`safe`) so they cannot. (8) `InterceptAssessment` carries no `confidence`, but A7's RaiseOrMerge read `assessment.confidence` (only the test fakes had one): it would have crashed on any real assessment. FIXED: an alert's confidence is the target's own probability from the forecast's location level (the forecast's overall confidence is the DISTRICT level's, near 1.0 for every target, which made all alerts look equally certain). (9) get_stats/`elapsed_min` convention: elapsed is minutes since the complaint was REPORTED (reported_event_at), matching B's tests.
[A11-prep] — DEFECTS FOUND IN TRACK A'S OWN EARLIER WORK BY RUNNING REAL DATA, all fixed: (a) severity was always LOW then always CRITICAL: bands recalibrated in config/policy.yaml (critical 9.0, high 5.0, medium 2.5 against a score that runs about 1.2 to 14.5; reasoning is in the file) and pinned by a test. (b) A merged alert extended its window but not its expiry, so it could expire before its own window ended. (c) on_cluster_merged never compared against the SURVIVOR's own open alerts, and re-keyed with a dedup kind that no longer matched creation: it now compares all involved alerts and keeps the kind as created. (d) DOC 3's 'close the newer alert as merged' contradicts the alert state machine (OPEN -> CLOSED is illegal): `Alert.close_as_merged` is an explicit, documented exception. (e) alert_notice idempotency keys used the MASKED account ref, so two accounts of one bank with the same last four digits (or refs shorter than four) collided on a unique index and failed the alerting stage: keyed by account id now. (f) Alert target_name was blank and target_kind was 'location' instead of ATM/BRANCH/AGENT: taken from the registry.
[A11-prep] — Still open, decided, or out of scope: (1) AlertUpdated is still not projected by analytics (needs a per-alert ledger table, a DOC 2 / LC-10 change); the `cluster_id` heatmap filter and a filter-options endpoint likewise need DOC changes. (2) RefreshOpenAlerts (periodic) is still a no-op: only a hop arrival refreshes forecasts; new cash-outs change the NEXT complaint's forecast, not the open ones. (3) A cash-out reported late after a miss was already called does not overturn it (one reconciled outcome per alert). (4) forecast v1 (B6) needs a model store: build_pipeline takes `model_store=` but main.py passes none, so the API runs the heuristic scorer until B's Trainer/ModelStore are wired (Track B's B6 note says Track A wires the /train job). (5) The forecast's `delays_min` come from intake (credit-to-cash-out at the complaint's own accounts); cashout_location_recency and cashout channel counts are only as good as graph's stats (recency is left empty). (6) The demo users' placeholder scopes still match no real region or bank id.
[Sync5] — JOINT RUN, agent side, real processes over HTTP (API on uvicorn, apps/bank-sim on tsx, a fresh SQLite for each): (1) POST /ingest of registry, complaint, hops, cash-outs and a second complaint -> the live chain raised a real alert; (2) an investigator's request_hold -> 201, action pending, a hold_request delivery and an alert_notice delivery queued; (3) the outbox worker sent both to bank-sim: status `sent`, attempts 1, provider `bank-sim` (the HMAC signature, timestamp window and Idempotency-Key were accepted by the real verifier); (4) bank-sim's console listed the request and showed the proposed amount; (5) Apply on the console (1,400,000 of 1,500,000 paise) -> a callback the API accepted -> action `applied`, `alert.hold.applied` on the alert timeline; (6) Release -> a second callback -> action `released`. The scripted driver and logs are in the session scratchpad (joint/drive.py, run2.txt); a screenshot of the console is NOT included and the run was on one machine, not the VM. `npm run ci` green: API 598 passed, 19/19 contracts kept, web green, bank-sim 14 passed.
[Sync5] — DEFECTS THE JOINT RUN FOUND (this is what Sync 5 is for; each would have passed every single-side test): (A, Track A, FIXED) bank-sim's fallback sim time is the latest webhook's `sim_time`, i.e. the instant the hold was created, so an operator acting at once reports the SAME sim time, and A8's 'strictly newer' rule silently discarded the first callback (the API answered 200 and the action stayed pending). `Action.apply_callback` now settles equal sim times by progression (pending < applied/rejected < released): a forward step is accepted, a duplicate or backward step ignored; an older callback is still ignored. Unit tests added. (B, Track A, FIXED) bank-sim sends `Authorization: Bearer <key>` on its callback while the API only read `X-Nakabandi-Service-Key`: LC-6 says only 'service key', so the callback route now accepts either (the human had started this edit; it is tidied and tested). (C, Track C, FIXED IN A SEPARATE COMMIT for Priyam to keep or replace) three defects in C2's console that only a real run shows: GET /console used `SELECT DISTINCT ON`, which is PostgreSQL-only, so the request list returned 500 on SQLite; console-list.ejs nested backslash-escaped template literals inside `${}` (a syntax error, also a 500) and inserted values unescaped; and server.ts called main() on import, so a second test file that imports createApp() tried to bind the same port. Nothing tested the console routes before: console.test.ts is new. NOT committed and NOT mine: the human's callback.ts edit (now redundant), the web package.json/package-lock lightningcss entry (machine-local), and Track D's docs/research file.
[A11] — WHAT THE STRESS RUN FOUND (the run is the point; every one of these passed all single-request tests): (1) SQLite 'database is locked': with Python's default deferred BEGIN, two requests that read and then write make the second fail INSTANTLY (a stale WAL snapshot cannot be upgraded; no busy timeout applies): 334 of 500 requests were 500s at first. FIX: every transaction now begins BEGIN IMMEDIATE with busy_timeout 30 s and synchronous=NORMAL (shared/infrastructure/db.py; `create_sqlite_engine(url, immediate=False)` keeps deferred transactions for a read-only engine). tests/unit/test_sqlite_concurrency.py proves it AND has a control showing the deferred default really fails under the same load. (2) The analytics projector was 78% of a complaint's time: one rollup row per candidate location (about 200) with a get-and-flush each, and an unbounded table. FIX: a set-based upsert (one select, one insert batch) and only each forecast's top 25 items per level are projected (PROJECTED_ITEMS_PER_LEVEL; the tail is negligible mass; named in code, not policy.yaml, because LC-7's keys are frozen). (3) Budget ranking loaded every alert of the shift's queue WITH timelines and saved every peer on every raise: O(alerts^2). FIX: rank over a five-column query, and persist only flag changes; `budget_rank` is now the alert's place WHEN RAISED (a live rank would rewrite the whole queue on every arrival; nothing reads it, only is_deferred / is_probe matter). (4) The alert clock re-registered every alert's timers on every one-second tick, loading each alert with its timeline while holding the write lock, and its timers were bound to request sessions. REDESIGNED as database-driven: EscalateAlert / ExpireAlert / ReviewLien / ReconcileOutcome each have `fire_due()`, which asks the database for what is due (indexed: ix_alerts_status_window_start, ix_alerts_window_end, plus expires_at) and acts on those, at most 200 per class per tick. There is nothing to register or rebuild, so restart-safety is by construction; RebuildTimers, the per-alert scheduler timers and AlertService's `scheduler` parameter are gone (shared.Scheduler remains in the kernel, now thread-safe with `has_due`, unused by alerting). An ACTIONED alert never expires (the query excludes it). (5) shared.Scheduler was not thread-safe (a request thread scheduled while the timer thread fired): locked.
[A11] — Built: shared/ratelimit.py (SlidingWindowLimiter; login's 5/min now uses it), shared/metrics.py (Metrics, MeteredEventBus, nearest-rank percentiles), maintenance.py (StreamGate + the 25-stream cap with 503 + Retry-After, AutoPause, seconds_until, create_schema/drop_schema, load_seed_file, reset_database), ops.py (GET /system/metrics, requires SIM_CONTROL: demo_operator and admin; LatencyMiddleware as plain ASGI so SSE is never buffered), scripts/stress.py (npm run stress), scripts/reset.py (npm run reset; offline, stop the API first), pipeline stage timers (`stage.<name>` and `stage.pipeline.total`), and restoring the in-memory SimClock at boot from the newest ingest batch (intake.latest_ingest_sim_time). Settings (all env, all OFF unless set; NAKABANDI_HOSTED_DEMO=true supplies the DOC 2 §2.7 defaults: 25 streams, control-gate rate limit 30/min per client (the DOCs give no number), auto-pause after 15 min, nightly reset 03:00 UTC): MAX_SSE_STREAMS, CONTROL_RATE_PER_MIN, AUTO_PAUSE_AFTER_MIN, NIGHTLY_RESET_AT (empty switches it off), SIM_CONTROL_URL, SEED_FILE, DEMO_USERS_ENABLED (default TRUE so local, offline and hosted DEMOS work; false for any non-demo deployment). The control-gate limit is on GET /auth/check, because every /sim-control call is authorised through it (DOC 2 §2.4). The simulator speed cap is world-sim's own (its model rejects factor > 60); the API adds nothing. Auto-pause only pauses a `running` world (world-sim's states: idle, running, paused, stalled), once per absence, and never overrules someone who resumed it.
[A11] — A DEPLOYMENT BUG FOUND AND FIXED: the API imports httpx at runtime (the bank webhook channel since A8, now maintenance.py and main.py) but declared it only in the root DEV group, and the image installs `--no-dev`: the container would have crashed at boot with ModuleNotFoundError. Declared in apps/api/pyproject.toml. Verified in a clean `--no-dev` environment built like the image (import OK), with a control that fails when httpx is removed. Dockerfile.api now also copies data/seed/mini_ingest.jsonl (the nightly reset reloads it; without it a hosted reset would restore an EMPTY world). Also: a stray file, apps/api/src/nakabandi/alerting/repos_dummy (a copy of reconcile.py left by a mistyped mutation command), was committed in A10 and is removed. The nightly reset is IN-PROCESS (a lifespan task), not a cron container: workers skip while it holds `app.state.maintenance`; requests in flight during the second or two it takes may see errors. It does not reset bank-sim's own store.
[A11] — Honest limits: the numbers above are one laptop, not the VM, and the machine was at load average 25-80 for earlier runs (the first results were pessimistic for that reason too). 50/s is about the ceiling of ONE Python process on one SQLite writer: at 50/s the p99 is 6 s and the max 20 s because writers queue; the DOC's 'p95 under 2 s ingest to alert' holds at the stress rate (client p95 1.5 s) and comfortably at the design volume (0.09/s). Every stress alert lands in one district-shift, the worst case for budget ranking. Outbox retries against an unconfigured bank inflate the `failed` count in the metrics (1122) and add write load; a real run should point NAKABANDI_BANK_WEBHOOK_URL at bank-sim. Seeded complaints (reset, `npm run seed`) go through intake without the live chain and stay `unprocessed` until the 5-minute retry job. A Track B observation worth passing on: graph's location stats and the forecast v1 model store are still not wired for training (Trainer is not scheduled).
[CHECKPOINT7] — Ran the DOC4 §4.3 verification via `docker compose` (infra/) on this machine as a stand-in — no cloud VM is provisioned yet; confirmed with the Systems lead before proceeding this way. First attempt bind-mounted /data to the Windows host for --db access, which produced spurious `sqlite3.OperationalError: database is locked` in the API itself (~8 occurrences, well after recovery) and a `database disk image is malformed` on a host-side read: this is SQLite WAL over a Windows virtiofs/9p bind mount, not a real bug — a named Docker volume (the actual compose file's own setup) does not reproduce it. Redid clean: named volume, api port published only for the host-side stress client, api container killed ~22s into a 50/s stress run (docker kill = SIGKILL), restarted via `docker compose up -d api`. Health was unreachable ~29s (14:13:25–14:13:54 IST), then metrics uptime_s reset confirming a clean reboot and pipeline stage counts resumed climbing immediately. Post-run DB check (docker cp'd out, read-only): 1701 complaints stored, 100% `processed`, 0 duplicate external_refs, 0 duplicate open alerts; client-tallied `accepted` was 1695 (6 lower) because those 6 responses were lost to the abrupt disconnect after the write had already committed — the safe direction (no accepted complaint was ever lost), not a defect. Outbox/webhook delivery was 0 sent / high failed+dead all run: confirmed root cause is infra/.env (Systems lead just added it) only carrying 7 of .env.example's ~23 keys — NAKABANDI_BANK_WEBHOOK_URL is unset, matching A11's own note above. Not yet done: the same sequence on an actual hosted VM once one exists (this only re-verifies the correctness properties under real container kill semantics, not the network/DNS/TLS parts of "hosted"). STATUS left at CHECKPOINT pending Systems lead confirmation — did not self-advance to ACTIVE/A12.
[A12-web-integration] — Directed by the Systems lead, cross-track: Priyam's merged Track C UI (feat/track-c, C1-C9) was almost entirely fixture-driven — including login itself (client-side React state, no real session) — per DOC4's own STUB/MOCK STRATEGY notes for steps that had, by this point, landed. Rewired feature by feature to the real API rather than editing Track C's files blind: generated a REAL apps/web/src/shared/api/schema.d.ts from the live FastAPI OpenAPI document (scripts/gen_web_openapi.py, `npm run types` now runs openapi-typescript — closes SYNC 4's "no OpenAPI -> schema.d.ts step"), added apps/web/src/shared/api/{enums.ts, types.ts} as a hand-kept convenience layer over it (the backend intentionally types severity/status/etc. as bare `str` at the API boundary — DOC 2 §2.4 looser coupling — so these aren't generated, and must be kept in sync with packages/contracts/src/nakabandi_contracts/enums.py by hand). Real auth (POST /auth/login + GET /auth/me, HttpOnly cookie), alerts (dropped a silent try/catch-to-fixture fallback that would have masked a broken backend), cases/clusters (the fixture's shape was richer than DOC 3 S1 and A12's real API — a workflow `status`, per-account role/volume, named locations, embedded graph_data — dropped or defaulted honestly, not fabricated; CaseDetail now composes the cluster graph from GET /clusters/{id} itself), audit (added `hash` to AuditEntryResponse — audit's own module, so no cross-track concern — so the page can show more than pass/fail), ops (GET /system/metrics has no history/per-channel/sim-control fields; eps_series is a session-local client-side accumulation from each poll, not fabricated history; sim-speed/channel-health/auto-pause sections are hidden, not faked), outbox (real DeliveryStatus values: pending/sent/failed/dead, not the fixture's invented delivered/retrying). evaluation is LEFT on its fixture and honestly re-labelled: no track ever registered a live evaluation endpoint (apps/api/src/nakabandi/evaluation/ is an offline harness only) — that's new backend work, not a rewire, and out of scope here. map and demo were already calling real endpoints (heatmap/regions; /sim-control/* proxy, /auth/demo-users) and were left alone beyond import-path fixes. Evidence: `npm run ci:web` green (eslint, tsc, 119/119 vitest, vite build), `npm run ci:bank-sim` green (14/14, unaffected), `npm run ci:py` 663/664 (the one failure is test_recovery.py, the same pre-existing timing-sensitive flake from Checkpoint 7 above — passes on retry). Not done: apps/web/src/features/map's two `apiClient.GET as unknown as ...` casts (pre-existing, not touched) could now be properly typed against the real schema instead of bypassed; left as a follow-up since map already works.
[A12-demo-polish] — Directed by the Systems lead after reviewing the live dashboard: the backend was wired correctly (previous entry) but the UI itself wasn't presentable — raw ids/codes, unstyled dropdowns, a black-looking Map page, and only 2 alerts in the database because nothing had ever actually run world-sim's live simulator. Fixed in five workstreams, all under `npm run ci:web` green (18/18 files, 119/119 tests) plus `npm run ci:py` 577/577 (demo_users.py rename verified against the real suite) and 19/19 import-linter contracts kept throughout.
  DATA STOCK-TAKE (the user pulled a new commit into data/ mid-session, `1f6bce4 chore(data): import seed and geo research datasets`): a large, properly-licensed real-world reference set (55 RBI banks, 158 Census-2011 districts, 8,505 OSM ATM/branch/agent locations — ODbL, needs README/footer attribution — 971 police stations, complaint-category priors, mule-cluster archetypes, sim calibration params). Checked: NOTHING in apps/ references any of these files yet; they are all world-sim `registry` INPUT data (Track B's own module, feeds the API only through `/ingest/registry`, same path Workstream 1 below fixes) — writing them into the DB directly would create a second source of truth, so that wiring is flagged as a Track B follow-up, not done here. Also found: the new files use JH (Jharkhand) as the 4th demo state where every existing piece of code (world-sim's registry.py, the web app's demo_states.geojson/useRegions.ts) hardcodes RJ (Rajasthan) — a real DOC-level "which 4 states" decision, flagged to the Systems lead, NOT silently resolved either way. The one safe, in-scope piece taken: `demo_users_seed.csv`'s realistic named personas (Aarav Kulkarni, Ritu Verma, Shadab Qureshi, Meena Iyer, Kabir Anand, Nandini Bhosale) replaced the placeholder "Demo I4C Analyst" etc. names in access/application/demo_users.py — names only, scopes/passwords left untouched (the CSV's ids, e.g. `BNK-001`, don't match world-sim's own scheme, and several tests hardcode the existing `demo-state-1`/`demo-district-1`/`demo-bank-1` scope ids directly).
  WORKSTREAM 1 (data flowing): infra/Dockerfile.worldsim's CMD was `python placeholder.py` (DOC 4 Step A5's literal placeholder, never swapped once B5 landed) — world-sim's real `worldsim live` control server had never actually been run anywhere, hosted or local, which is the real root cause of "only 2 alerts" (data/seed/mini_ingest.jsonl is a deliberate 5-row smoke fixture, not a demo dataset, working as designed). Fixed: Dockerfile.worldsim now builds and runs `uv run worldsim live` for real (mirrors Dockerfile.api's build pattern); docker-compose.yml's world-sim service gained `WORLDSIM_API_URL=http://api:8000` / `WORLDSIM_SERVICE_KEY=${API_SERVICE_KEY}` (reusing the SAME machine-client secret bank-sim already reuses, DOC 4 §4.4 — no new secret needed) and `depends_on: api: condition: service_healthy` (world-sim POSTs its registry at startup, needs the API up first); added root script `npm run dev:sim`; README rewritten with an actual "Running the full stack locally" section (was "Scaffold only", stale since roughly A5).
  WORKSTREAM 2 (Map page): three real bugs, not just CSS. (1) `maplibre-gl/dist/maplibre-gl.css` was never imported anywhere (now imported in main.tsx) — MapLibre needs its own stylesheet to size its internal canvas/controls; likely the main cause of "all black". (2) `MapLibreAdapter.setupSourcesAndLayers()` ran inside the `"load"` callback with NO try/catch, so any throw there left `init()`'s promise pending forever with nothing logged — a silent, permanent hang independent of cause (1); now wrapped, logs and rejects cleanly. (3) alerts and bank-infrastructure locations were never fed data at all — real sources/layers existed (MapLibreAdapter) and real GeoJSON builders existed (layers/alertsLayer.ts, locationsLayer.ts) but nothing in MapPage.tsx ever called them: added `useLocations.ts` (GET /geo/locations) and wired both `useAlerts()` and it into the map via `setLayerData`, with a merged `targetCoords` map (location id, cell id, AND district/state region id, since an alert's target can be any of the three). Also fixed: the initial `fitTo` call ran inside the `[filters.state]` effect, which only fires on a LATER filter change (never on first mount, since `adapterRef` isn't ready yet when that effect's mount-run happens) — moved the first framing call into the `init().then()` callback itself.
  WORKSTREAM 3 (dropdowns): every `<select>` in the app already used a consistent `nk-select`/`nk-select--sm` class (FilterPanel, AlertsInbox, AlertDetail, DemoConsole) — but `.nk-select` was never DEFINED anywhere, so all of them rendered as bare browser-default selects; a pure-CSS fix can't touch the open option list of a native select at all (the actual "looks unfinished" part), so per the approved plan this pulled in `@radix-ui/react-select` + `@radix-ui/react-tooltip` (headless, unstyled — every pixel of both the trigger and the open list is themed in tokens.css's new "Form Controls" section) plus `date-fns`. New `shared/ui/Select.tsx` / `Tooltip.tsx`; replaced all 11 native selects across FilterPanel/AlertsInbox/AlertDetail/DemoConsole/CasesPage/ClustersPage (the latter two had ALSO misused `nk-input` with hand-rolled inline dark-theme styles instead of `nk-select`); added `.nk-input`/`.nk-textarea`/`.nk-checkbox` base rules too (also previously undefined — every text input/textarea in the app was unstyled, not just selects) and applied `.nk-checkbox` to the two previously-unclassed checkboxes. `TooltipProvider` added to app/providers.tsx (real app) — jsdom needs `hasPointerCapture`/`scrollIntoView` polyfilled for Radix to open in tests at all, added as `apps/web/src/shared/test/setup.ts` wired via vite.config.ts's new `test.setupFiles`. 4 existing tests that drove a native select via `fireEvent.change` (MapPage×2, AlertsInbox, DemoConsole) rewritten to `userEvent` click-based open/select, since Radix's trigger is a button+portal listbox, not a real `<select>`; one test file (ClusterGraph.test.tsx, whose `renderWithProviders` helper is shared with a ClustersPage render) needed `TooltipProvider` added or every render there throws "Tooltip must be used within TooltipProvider". One genuine, unrelated discovery while chasing a flaky failure here: DemoConsole's lazy-loaded route chunk got measurably heavier (19 KB → 38.5 KB gzipped) from the new deps, which pushed `DemoRouteAuth.test.tsx`'s dynamic-import-plus-Suspense resolution past testing-library's default 1000 ms `findBy` timeout under load (reproduced deterministically 3/3 in isolation, and confirmed NOT a real hang by raising the timeout to 10 s — resolves in ~2.4 s); fixed by giving that one assertion an explicit 10 s timeout rather than leaving a load-sensitive flake.
  WORKSTREAM 4 (shared display layer): new `shared/lib/roles.ts` (`ROLE_LABELS`/`roleLabel()`, previously hand-duplicated identically in Shell.tsx and LoginPage.tsx) now also used by AuditPage's actor column; new `shared/lib/format.ts` additions `humanizeStatus()` (replaces two fragile single-occurrence `.replace("_", " ")` calls in CasesPage/CaseDetail; special-cases FIR/KYC/UPI/ATM/SIM to stay upper-case), `humanizeAction()` ("Namespace: Verb" for AuditPage's mixed-source action codes — alert./outcome./action./etc — instead of shouting the raw dotted code in caps; AlertDetail's OWN single-namespace timeline reuses `humanizeStatus` directly since repeating "Alert:" on every row of that alert's own timeline would be redundant), and `shouldShowKindBadge()` (skips the kind badge when a target's name already contains it — the "MG Road ATM ATM" duplication; checked AlertDetail's own target section too, which turned out to already be two clearly-labelled KeyValue rows, not a real duplication there). `ClusterGraph.tsx`'s two inline `(paise/100).toLocaleString("en-IN")` calls replaced with the existing `formatInr()` (an existing test at line 249 already expected `formatInr`'s exact "₹45,00,000.00" output, so it had apparently been failing or untested against the actual render before this). OutboxPage/OpsPage's hand-rolled `toLocaleString` calls replaced with the shared `formatSimTime()`; DemoConsole's manual `.slice(11,19)` timestamp replaced with `date-fns`'s `format()`. Removed 3 leftover internal DOC-section references from user-visible copy (AlertDetail, CasesPage, FeedbackPanel) — meaningless to an end user.
  WORKSTREAM 5 (partial — time-boxed, not exhaustive): Shell.tsx nav items gained a `description` + `title` tooltip attribute each; OutboxPage's `rendered_body` label renamed to "Message preview" and `alert_id` is now a link to that alert; OpsPage's "Outbox Depth" stat card and "Outbox Channel Health" section both link to /outbox; ClustersPage gained a page-level title/subtitle explaining what a cluster is and a Tooltip on the Novelty badge; CasesPage's empty state gained a "Reset Filters" button (only shown when a filter is actually active, not on a genuinely-empty list). NOT done, deferred rather than rushed: swapping CasesPage's hand-rolled `<table>` to the shared DataTable component, a visible next-action on CaseDetail (FIR recommendation), and per-metric tooltips beyond ClustersPage's Novelty badge (Confidence/Ladder etc.) — these touch more surface area per item and are left for a follow-up pass.
  WORKSTREAM 6 (LLM-generated plain-language alert explanations): discussed and scoped in the plan (server-side-only NVIDIA-NIM call, locked system prompt enforcing DOC 1 §1.5, optional env vars, graceful no-key degradation) but NOT built this session — deliberately deferred pending the Systems lead's go/no-go and, if yes, the NVIDIA NIM key.
  Two test-router-context bugs caught only by running the full suite, not by editing in isolation: OutboxPage.test.tsx and OpsPage.test.tsx both rendered their page directly (no `<MemoryRouter>`) since neither page used `<Link>` before this session; both now need one, since OutboxPage's alert_id and OpsPage's Outbox Depth card do now.
[A12-sim-alignment] — Discovered live-streaming root cause during localhost inspection: `mini_ingest.jsonl` seed complaints were dated 2026-01-15, setting the API's SimClock to 2026, while `worldsim`'s `_SIM_EPOCH` was hardcoded to 2025-01-01, causing all live ticks to be dropped as non-monotonic. Updated `worldsim/writer/batches.py` to default `_SIM_EPOCH` to 2026-01-15T06:00:00Z (with `WORLDSIM_EPOCH` override support) and added `@router.post("/cashouts")` alias in intake `routers.py` to match `runner.py`'s endpoint. Verified live stream: complaints, clusters, and alerts now flow continuously at 60x speed with 8,506 real locations.
[A12-frontend-strategy-revamp] — Directed by the human against a new root-level doc, `NAKABANDI_Frontend_Product_Strategy.md` (a critical redesign brief, not a DOC1-4 file — Integration-Owner-authored, not edited by this session). Implemented its §10.1 "Essential" and §10.2 "High value, cheap" tiers, all frontend-only wiring/framing against data the API already returns (zero backend changes). Web app entered this session with Track C fully merged (CHECKPOINT 8) and unrelated in-flight uncommitted map work already present (MapPage.tsx, MapLibreAdapter.ts, Legend.tsx, useHeatmap.ts, TimeSlider.tsx, MapAdapter.ts, vite.config.ts, plus new data/india_outline.json, layers/heatmapLayer.ts) — left untouched except HotspotDrawer.tsx (one ULID fix below); its one pre-existing test failure (MapPage.test.tsx, Legend text) and 6 pre-existing lint errors (react-hooks/exhaustive-deps rule missing, unused `_f`/`_t` vars, one `@ts-ignore`) are confirmed NOT caused by this session (git diff showed them present before any edit here) and were left for whoever owns that in-flight work.
  §10.1 Essential: (1) demo console reliability — did NOT touch world-sim/the underlying stall cause (out of frontend scope, likely Track B/infra); fixed the OTHER half explicitly named in the doc, error LEGIBILITY: DemoConsole.tsx's raw `Engine Error: {last_error}` banner (e.g. "HTTP 500" with zero next step) replaced with a plain-language headline, a "Retry Status Check" button wired to the existing `refetchStatus`, and the raw string demoted to a collapsed `<details>` "Technical detail". (2) FeedbackPanel.tsx's hardcoded "Prediction Assessment" panel ("Velocity & Hop Cadence +38%" etc, static strings never wired to any field — DOC2 §2.8 risk T12: this is the exact feature-attribution framing the architecture warns against) replaced with narrative `forecast.evidence[].text_en` cards, verbatim, no percentages; component now takes a real `evidence: EvidenceModel[]` prop instead of only `confidence`. (3) AlertDetail.tsx's action bar rewired from hardcoded `can(principal, "REQUEST_HOLD")` etc (client-side permission table) to the server's own per-alert `allowed_actions: string[]` (LC-4, `alerting.__init__.AlertService.allowed_actions`, already computed, was returned but never read) — a correctness fix, not just UX: closes the bank_nodal/REQUEST_HOLD frontend question by construction, whichever way the backend permission itself is later resolved. (4) New `ProportionalityStrip.tsx` (disputed/proposed amounts, ratio as a bar via the existing `ConfidenceBar` — already visually bounded at 100%, review/expiry timestamps, magistrate-report reminder line) and `LadderDecisionPath.tsx` (verdict + ladder badges, a window-remaining-vs-ETA two-bar comparison, `reason_code` in plain language) — both read `alert.interception.at(-1)`, shown together under a new "Interception Assessment" section; wording for the magistrate line matches DOC1 §1.2 M6's own acceptance-criteria phrasing exactly, no invented statute citation. (5) Raw ULIDs demoted from primary identifier to a secondary/muted line, `cluster_ref` (or case `cluster_ref`) promoted to primary, in: AlertsInbox.tsx's table column, ReviewQueue.tsx's triage card, HotspotDrawer.tsx's contributing-alerts list, AlertDetail.tsx's drawer title (now `{cluster_ref} · {target name}`, was `Alert {ULID}`), and CaseDetail.tsx's headline (was `Case Dossier: {case.id}`, now `{cluster_ref}`); CasesPage.tsx and ClustersPage.tsx were already correctly labelled on inspection (two distinct, clearly-headed columns) and left alone; Outbox intentionally left showing raw ids (doc's own §7.7: "a table with expandable rows is right for a debugging surface").
  §10.2 High-value/cheap: (6) new `features/dashboard/api/useAnalytics.ts` (`useLiveMetrics`/`useTimeseries`, polling since no SSE event exists for either) wired into DashboardPage.tsx: the sparkline's literal `// Fake 12-hour trend buckets` hardcoded array replaced with real `GET /analytics/timeseries` points, and a new "Forecast Mass (24h)" KPI card added from `GET /analytics/live-metrics`'s `expected_mass`/`active_locations` — labelled explicitly as "probability-weighted cash-out risk", NOT rupees (checked the backend: `analytics/application/project.py`'s `mass_from_forecast` confirms `expected_mass` is a forecast-probability aggregate, not money; mislabelling it as INR would have been exactly the kind of overclaim this whole doc argues against). Existing Open Alerts/Critical/Active Clusters/Total Disputed/Peak Intensity KPIs were already real (client-derived from real endpoints, just not the analytics module's own aggregation) and left as-is. (7) new `TimingDecayCurve.tsx` (Recharts AreaChart, four points at minute 0/30/60/120 from `forecast.timing.{p30,p60,p120}`, a "Now" ReferenceLine at `elapsed_min`) — no new dependency, matches the ColdStartChart.tsx pattern already in the codebase. (8) new `EvidencePackPanel.tsx` + `useBuildEvidencePack` mutation wire the three previously-100%-unused endpoints (`POST /alerts/{id}/evidence-pack`, `GET /evidence-packs/{id}`, `GET /evidence-packs/{id}/download`) directly into Alert Focus: one button, then SHA-256 and audit-head-hash shown in full (not truncated, not hidden behind the download link), a `<a href={download_url}>` (already an absolute same-origin `/api/v1/...` path from the backend, confirmed in casework/interfaces/routers.py — no prefixing needed). (9) `shared/ui/Timeline.tsx` gained an `orientation="horizontal"` prop (default stays "vertical", so CaseDetail.tsx's existing usage is unchanged); AlertDetail.tsx's "Evidence & Audit Trail" now renders `timeline[]` horizontally. Found and fixed in passing: the vertical timeline's dot color CSS only defined `--warn`/`--error`/`--success` modifiers while AlertDetail.tsx had always passed `--warning`/`--info`/`--success` — `--warning` and `--info` dots were silently uncolored since whenever this was built; added the missing CSS (both variants now, vertical and new horizontal). (10) ULID legibility folded into item 5 above.
  DEFERRED, not started: §10.3 (small backend extensions — unit lat/lon on `best_unit` for the Interception Radar; a thin `GET /evaluation/runs[/{id}]` router) needs backend coordination (interception/geo are Track B's domain; evaluation's hidden-truth firewall is an explicit invariant) — flagged for the Integration Owner/Algo dev, not built unilaterally. §10.4 (5-destination IA restructuring — merging Clusters/Cases, tabifying Evaluation/Ops/Audit under System Integrity, demoting Outbox/Dashboard; fund-flow directed timeline via Cytoscape breadthfirst; hierarchical zoom-bound map/Interception Radar) explicitly gated by the doc's own §10 sequencing on §10.3 landing first, and touches routing/nav files that overlap the in-flight uncommitted map work noted above — not attempted this session to avoid compounding two half-finished changes on the same files.
  Evidence: `npm run typecheck` clean; `npm run lint` — same 6 pre-existing MapPage.tsx/MapLibreAdapter.ts errors before and after (git-diff-verified), zero new; `npx vitest run` — 118/119 passed, the 1 failure is the same pre-existing MapPage.test.tsx Legend-text assertion (present before this session's edits); `npm run build` succeeds (same pre-existing >500kB chunk-size warning, unrelated). Updated `AlertDetail.test.tsx` (title assertion, "hides action buttons" test's wait-for text, and a new `allowedActionsFor()` fixture helper mirroring the backend's `_PERMISSION_FOR` table so existing permission-named test cases still drive the right `allowed_actions`) and `CaseDetail.test.tsx` (one string literal) to match; both still pass. No `npm run ci` (combined py+web+bank-sim) run this session — only the web-side checks above, since nothing outside apps/web was touched.
  Not committed: left for the human to review and stage (CLAUDE.md: commit only when explicitly asked). `docs/state/track-a.md` and this entry are the only state-file change.

## Frontend overhaul (docs/plans/frontend_overhaul_plan.md) — Integration Owner override of apps/web
Explicit user instruction ("proceed with implementation plan"), executed on feat/track-a; apps/web is normally Track C's. STATUS above (A12 CHECKPOINT) is unchanged by this work. One commit per phase, never pushed.

- Phase 1 — Foundation, Reskin & HUD Chrome: DONE. Tailwind stood up from zero (tailwind.config.ts, postcss.config.js, shared/tokens/tailwind.css; **preflight disabled** because tokens.css ships its own reset and preflight would change element defaults under ~4,000 lines of nk-* rules). tokens.css `:root` value layer replaced with the tactical dark theme, token NAMES unchanged, additive tokens (surface-elevated, text-tertiary, accent, glows, weight tiers). Legacy numeric weight tokens (500/600/700/800) now alias onto the 510/590 tiers so nothing renders heavier than 590. Radii tightened (4–10px), shadows reduced to hairline borders. Verdict/ladder/status badges no longer borrow severity hues: neutral fill + weight, highest-consequence states inverted to a light chip. Fonts bundled (`@fontsource-variable/inter`, `@fontsource-variable/jetbrains-mono`); Google Fonts `<link>`s removed from index.html (they were a runtime network dependency). Shell top bar: SIM clock (sim.time-driven, "Z"), stream pill (glow only while streaming), "Press ? for shortcuts", decorative search input removed. New shared/ui/ShortcutSheet + shortcutRegistry (scopes register while mounted; Phase 2/3 register theirs). Map basemap switched Positron -> OpenFreeMap `dark` (verified 200 and near-black background) so /map is not a pale rectangle in a dark app; it is still an online tile source (pre-existing).
- Evidence: `npm run ci:web` green (lint, typecheck, 116/116 vitest, build). Built CSS contains Tailwind utilities from the preset (`.text-nk-text-tertiary`, `.flex-1`, `.lg\:inline`), woff2 files are emitted, no `googleapis` string in dist. Screenshots of /alerts, /map, /cases taken with Playwright against the dev server (API mocked): dark theme, HUD and shortcut sheet render.
- Known leftovers for later phases (not regressions): AlertsInbox KPI tiles and Dashboard tiles still pass hardcoded green/blue value colours (Phase 2 handles the Golden Color Rule there); the dark basemap tile source is remote.

- Phase 2 — Triage Cockpit & Live KPIs: DONE. New: shared/time/TimeProvider (one rAF loop anchored to sim.time ticks, rate from the last two ticks, stops extrapolating when ticks stop), shared/audio/{tacticalPing,soundPreference} (CRITICAL-only cue, AudioContext armed on first gesture, localStorage preference), shared/ui/{CountdownRing,HoldToActuateButton,RollingCounter,ConsequenceTally}, shared/lib/useMediaQuery, features/alerts/holdTally. DataTable: real virtualization (`@tanstack/react-virtual`, fixed rows, spacer rows; falls back to a first page when the container has no measurable height), functional column sort (it was cosmetic before), active-row tracking + imperative step/activate. AlertsInbox: j/k, 1-9, Space, f, d, x/Esc via react-hotkeys-hook (form fields ignored, off while an action dialog is open), two-pane layout >=1280px (AlertDetail `variant="panel"`), held-back new rows + "N new" pill, density toggle, sound toggle, tally. AlertDetail: request_hold/dispatch confirm by press-and-hold; sonner `toast.promise`; override toast is non-dismissible with an explicit Acknowledge action. useAlertAction is optimistic (status patched in every cached list/detail, rolled back on error). react-hot-toast removed, sonner added. Dashboard KPIs roll and no longer use hardcoded hex.
- PLAN CORRECTIONS found while building (recorded in the plan too): (1) NO UNDO for hold/dispatch: the alert becomes `actioned`, which is not in ACTIONABLE_STATUSES, so a compensating `override` returns 409 and would not release a lien anyway; the UNDO toast was dropped rather than shipped as a lie. (2) The pre-existing request_hold UI sent `lien_amount_paise` and no account, which the backend rejects (`LIEN_INVALID`); fixed by adding an account picker (from GET /clusters/{cluster_id}) and sending `account_id` + `proposed_paise`. (3) alert.created carries only {alert_id, version}, so the ping fetches the alert to read its severity first. (4) There is no dismiss action in the backend, so `x` only closes the detail pane.
- Evidence: `npm run ci:web` green (lint, typecheck, 140/140 vitest incl. new CountdownRing, HoldToActuateButton, TimeProvider, holdTally, ShortcutSheet, inbox j/k + sort tests and the rewritten hold test, build). Playwright screenshots against the dev server (mocked API + a sim.time SSE event) show rings, the two-pane layout, fixed-height rows and keyboard-active highlight. NOT verified end to end: the ping (no audio in a headless run), a real bank-sim hold callback reconciling the tally, and behaviour against a live backend.

- Phase 3 — Command Palette & Tactical Map: DONE. shared/ui/CommandPalette (cmdk; presentational) fed by app/layout/CommandPaletteHost (mounted only while open; composes alerts + cases hooks; groups: selected-alert context actions, Go to, Alerts, Cases, Accounts by masked ref, Banks, Commands), Cmd/Ctrl+K in Shell via react-hotkeys-hook (works from form fields), topbar trigger. shared/state/selectionStore (selection + one-shot pending action, so the palette can open the hold/dispatch dialog on the alerts screen; AlertsInbox writes the selection on row click and j/k). Map: LayerPanel (toggle + hotkey + live count per layer; heat/locations/alerts/route/boundaries; persisted to localStorage), MapHud (cursor lat/lon and zoom written to DOM refs, not state; layers/entities counters), `map` hotkey scope (H L A I B, Esc closes the hotspot drawer), pulsing radar on the top-3 hottest cells with value >= 0.5 (canvas StyleImageInterface; idle = no repaint; tied to the heat toggle), selection highlight ring + flyTo when an alert/location is selected elsewhere, alert/location marker clicks write the selection. MapAdapter gained onPointerMove/onViewChange/flyTo; heat colour ramp and legend recoloured for the dark basemap.
- Deliberate omissions: no viewport-scoped fetching (no bbox param on the heatmap API and the data is four demo states); no separate "static circle-radius transition" layer for the non-pulsing hot cells, because the existing GPU heat density + point layers already cover them at zero per-frame cost; dossier context commands (jump to hop N, isolate ring, export) land with Phase 4b.
- Evidence: `npm run ci:web` green (150 vitest incl. CommandPalette, selectionStore, hot-cell picking, LayerPanel; build). Playwright + SwiftShader WebGL screenshots of /map against the dev server (mocked API) show the dark basemap, tinted heat, pulsing markers on the hottest cells, layer panel counts, HUD readouts, and the palette resolving a masked account ref. Not verified: the radar's frame cost with the DevTools performance panel (headless), and the "alert -> Show on map" flyTo against real data.

- Phase 4a — Case Dossier Quick Wins: DONE. features/clusters/graphModel.ts (pure): `classifyRoles` (topological roles origin / pass-through / pooling (>=3 inbound, checked before origin/terminal) / terminal / isolated / summary, from in/out-degree over the edges already fetched) and `toCytoscapeElements` (the one API->Cytoscape seam). ClusterGraph now styles by role SHAPE (colour is not used for role), edge width by amount_paise, edge brightness by tempo, relabelled legend ("Origin (no inbound hops)" etc. — deliberately not "victim"/"mule", which this data cannot support), stacked legends, node click writes selectionStore. CasesPage: Panel + DataTable (DataTable gained testId/getRowTestId) + neutral CaseStatusChip (the old red fir_recommended pill is gone); CaseDetail/ClustersPage/ClusterGraph HTML re-themed onto tokens (no hardcoded hex left in them except Cytoscape's style array, which cannot read CSS variables and mirrors the tokens). Graph height 360 -> 480 in CaseDetail so the legends do not cover nodes.
- Evidence: `npm run ci:web` green (157 vitest incl. 7 new graphModel tests: role classification incl. the singleton and pooling cases, LEA vs masked labels, amount/edge fields). Playwright screenshot of /cases/CASE-1 with a realistic multi-hop cluster (mocked API): circles, diamond, rounded-rect and hexagons all render, edge widths differ. This is the regression check for the "every node the same grey circle" defect.

- Phase 4b — Case Dossier Forensic Depth: DONE. ClusterGraph migrated to `react-cytoscapejs` with `cytoscape-dagre` (LR, edge minLen from hop layer) and a `cytoscape-cose-bilkent` fallback when the loaded trail has a cycle (extensions registered once in clusters/cytoscapeSetup.ts; local .d.ts for cose-bilkent in shared/types/cytoscape-plugins.d.ts; all three licences read from the installed package.json: MIT). graphModel.ts is now the pure core: buildGraphView (hop + min-amount filters -> bank grouping -> 200-node cap; reports exactly how many edges and how many paise were hidden), groupByBank, capGraph, hasCycle, chainFromOrigin, nodeStats, edgesToCsv (escapes and neutralises =+-@ formula injection). UI: hop slider, min-amount slider, "N edges hidden (₹X) · show all", Group by bank, Isolate trail (dims everything that is not upstream/downstream of the selection), entity inspector (in/out degree and totals, first/last seen, chain from origin, NO risk score), Highlight chain, PNG/CSV working-copy export (file names and tooltips say "working copy — not the evidence pack"), Recharts Sankey tab (bank x hop columns; rings reported not dropped), timeline scrubber (local playback state, fades future hops). Palette gained a "Case dossier" group (show hops 1..N, isolate, export) via shared/state/dossierActions, registered by the mounted graph.
- Deviations: `cytoscape-expand-collapse` was installed then removed unused (grouping is a pure transform; reasons in the plan); "jump to evidence item N" from the plan was not built (the dossier has no evidence-item list to jump to); sub_communities grouping dropped (endpoint does not return them). Found: grouping same-bank accounts can create a false ring; the graph labels a real ring and a grouping artifact differently.
- Evidence: `npm run ci:web` green (172 vitest incl. 10 more graphModel tests, sankey/scrubber tests). Playwright screenshots against the dev server (mocked API): dagre left-to-right cascade, hop/amount toolbar, scrubber, tabs, inspector with correct totals (₹14,700 inbound = 900000+450000+120000 paise), isolate dimming, grouped view, Sankey with bank labels. Not verified: playback animation and PNG/CSV download in a real browser download flow; behaviour at the 200-node cap on a real cluster.

- Phase 5 — System Integrity HUD & consistency pass: DONE. shared/api/fetchEvalResults.ts (text -> sanitise NaN/±Infinity -> JSON.parse; metrics are `{value: number | null, n}`): the shipped apps/web/public/eval-results.json really contains bare NaN today (6 occurrences), so the Evaluation page was failing on `res.json()` before this. New shared/ui/{ProgressRing (null = "n/a" + "insufficient sample (n=…)", never 0%), Sparkline}. features/system/SystemHud (new "Overview" tab): audit chain status (intact / broken at entry #N + head hash), model-quality gauges + Brier score with an improving/worsening arrow from a localStorage history of the last runs (the backend keeps none), latency & throughput from a 10 s poll into a 30-sample buffer with sparklines per stage, exposure tiles with RollingCounter. Sections are gated by the endpoints' own permissions. Consistency: ReviewQueue and EvidencePackPanel use sonner `toast.promise` (evidence pack: plain outcome toast, no undo window, it is a sealed artifact); OpsPage chart colours moved to tokens; EvaluationPage lost its green/amber/red ring. e2e hero-flow spec updated for press-and-hold (button now "Hold to request lien").
- Deliberately not built: a calibration/reliability curve and lead-time distribution (no producer writes that data: plan §5.1).
- Evidence: `npm run ci:web` green (183 vitest incl. SystemHud: intact/broken chain, NaN-laden eval file, role gating, latency readouts, buffer cap, Brier trend; fetchEvalResults parsing). NOT RUN: `npm run test:e2e` (needs the full stack: API, world-sim, bank-sim), so the updated hero-flow spec is unverified; a live click-through of the demo flow against real services was not done.

## Frontend overhaul — completion summary
All five phases (1, 2, 3, 4a, 4b, 5) are committed on feat/track-a, one commit each, none pushed. Everything was verified with unit/component tests and Playwright screenshots against the Vite dev server with a MOCKED API; nothing was verified against the live backend/simulators. Open follow-ups: run `npm run test:e2e` and a manual demo click-through with `npm run dev:sim`; confirm the sound cue by ear; the map basemap is still an online tile source (offline Compose path); optional backend items in the plan §5 (5.3a hop/prune metadata, 5.5 evidence chip) are not started.

## Live Local Stack — findings and corrections (Integration Owner review of an antigravity session)
- Kept: Dockerfile.api `COPY models/`, Dockerfile.worldsim `COPY apps/api/` (workspace lock), `WORLDSIM_API_URL` ending `/api/v1` (the emitter appends `/ingest/...`), `../config` read-only bind mounts, api port 8001. Still to check: the bind mounts against the offline/hosted Compose path, and the duplicated 8001 mapping in docker-compose.override.yml.
- Reverted: the policy.yaml (abstain minimums, min_confidence_for_action, floor_confidence, severity_bands) and sim.default.yaml (lag) edits. They made 7 tests fail (test_live_chain x5, test_recovery, test_severity_bands_spread_realistic_alerts_across_all_four_levels) and overrode the Appendix A lag assumption. A demo-specific calibration must be a deliberate policy decision with the tests updated, not a silent edit.
- The earlier "44 clusters / 44 alerts verified" claim was an artefact of the bug below: every complaint on an injected cluster became its own one-account cluster.
- Root cause of single-account self-looping clusters: `/control/inject-cluster` created clusters with no accounts; fixed under Track B changes (see docs/state/track-b.md, [A/inject-cluster]).
- Schema drift: `create_all` does not migrate existing SQLite volumes; after pulling a schema change run the reset (`/control/reset` or drop the api-data volume).
- Docker volumes created before the fix still hold self-loop data; reset the api DB and restart world-sim.

## Cluster graph rework + live warm-up (Integration Owner session)
- ClusterGraph: parallel hops merge into one edge (×N, summed amount; `hop_count` is UI-only, not an API field); layered dagre layout runs over forward edges only (BFS depth from origins) and return edges are drawn as dashed pink arcs; role colours (origin green, pass-through blue, pooling amber, terminal red), tempo ramp orange→blue, node size by money moved; dense graphs (> 3 merged edges per account) show only each account's strongest flows with a visible "N weaker flows (₹X) set aside · show all" notice; Full screen button (fixed overlay, Esc exits). This overrides the earlier "colour reserved for severity" note in ClusterGraph.tsx's header for nodes/edges.
- Root data issue left as is: the simulator picks each hop's target uniformly among the cluster's accounts, so clusters are near-complete random digraphs. A layered, mostly-forward hop model would be a Track B realism change (alters the golden hash), not done here.
- Alerts: with the committed policy they appear once clusters have cash-out history (12 alerts after ~0.84 simulated days at 60×). No threshold change was made.
- `npm run ci` green: 689 py, 186 web, 14 bank-sim.

## UI/ops fixes + NIM case explanation (Integration Owner session)
- Alerts inbox: the four KPI cards became compact chips on the attention-budget row; the inline detail panel is tabbed (Target / Assessment / Trail / Evidence) so each tab fits without scrolling; drawer variant unchanged.
- Map: heat went blank because live alerts were too sparse for any district/cell to reach `heatmap.k_threshold` (3) and the first request used level=cell while the camera was at district zoom. Initial level now follows the initial zoom; when the live layer is fully suppressed the heat falls back to facility-level rows (never suppressed by the API, already shown as alert markers) with a visible note; potential-layer masses are normalised to the largest cell. The k-threshold itself is unchanged.
- ClusterGraph: controls and notices are in normal flow (they overlapped and blocked the Full screen button); the canvas is absolutely positioned in its box and resized by a ResizeObserver, which fixes the horizontal blow-up after leaving full screen.
- System Integrity overview: adds complaints/s, live streams, outbox breakdown and a per-stage latency table from GET /system/metrics (fields that were fetched but not shown). The model-quality gauges were blank only because eval-results.json had n=17 (< 30); re-running `scripts/evaluate.py` on the live data (n=35) refreshed apps/web/public/eval-results.json.
- NEW ENDPOINT (needs a DOC 3 entry via the Contract Change Process): GET /cases/{case_id}/explanation -> {available, text, model, reason}. casework/domain/explain.py builds anonymised aggregate facts (no account refs/ids) and the ethics guard (DOC 1 §1.5: no guilt/legal language, FIR disclaimer appended); casework/infrastructure/llm.py calls NVIDIA NIM (OpenAI-compatible) server-side; results cached per unchanged cluster. Settings: NVIDIA_API_KEY (unset/empty = feature off, brief only), NIM_MODEL (default meta/llama-3.3-70b-instruct), NIM_BASE_URL. Not exercised against the real NIM service (no key in this session); the client is tested against a local stand-in.
- Flaky under load: HoldToActuateButton "released before the hold completes" and AlertsInbox "filters by status tabs" each failed once while the Python suite ran in parallel; both pass alone (186/186 twice).
- NIM verified against the real service (model nvidia/nemotron-3-super-120b-a12b set in infra/.env): a reasoning model leaked its planning into `content` and was cut off at the token limit. Fix: request `chat_template_kwargs.enable_thinking=false` (retry without it on 400/422), max_tokens 700, strip <think> blocks, and the ethics guard now also rejects leaked-reasoning openers and > 220 words. Verified: clean 3-paragraph text on a live case (~5 s), rendered in Investigate.
- Alerts detail panel opens on the LEFT: docked half-width panel from 1100 px (grid order -1), and the slide-over Drawer now takes `side="left"` for narrower screens.
- Map heat coverage: (1) the live layer's default window is now the same 72 h as the time slider (analytics `_effective_window` uses policy heatmap.potential_lookback_hours for a live query without an explicit `from`; /live-metrics keeps its 24 h); before, only alerts of the last 24 sim hours lit up while markers showed all. (2) Alert markers are filtered to the same 72 h (by sim hour). (3) Whenever any area is suppressed by the k-threshold, the heat uses facility-level rows (not suppressed by the API, already shown as alert markers), so every alerted facility is lit.
- Environment note: after the PC restart world-sim came back at sim t=0 (speed 1x) while the API DB and clock are at ~18 Jan 2026, so the live feed is stalled (0 new complaints). Reset the api DB (or drop the api-data volume) and restart world-sim to run the demo forward.
- Trail scrubber ("Play trail") moved into ClusterGraph (clusters/TrailScrubber.tsx; cases/CaseTimelineScrubber.tsx re-exports it), so it appears on the case dossier, the Explore-cluster page and in full screen (slider min-width 60vw there). A caller can still drive playback with `playbackUntilMs`.
- Page navigation is a fixed bottom taskbar (same SideNav markup, CSS overrides at the end of tokens.css; --nk-taskbar-h 3.5rem; shell body gets bottom padding). The OSM attribution stays at its right end (hidden under 1100 px; the map's own attribution control remains).
- Map click intel dossier: clicking a facility dot, an alert marker or a heat point opens `IntelPopup` (features/map/IntelPopup.tsx) inside the map — classification banner + DTG, designation, threat band and score, position (DD/DMS, district/state, cell), facility profile (bank, area type, facilities within 5 km), linked trajectories (severity/status/ladder/confidence/countdown, Track and Open actions) and, for the most severe linked alert (or the clicked one), a cash-out & interception brief from its own detail (expected cash-out, residual mass, verdict, P(intercept), nearest unit ETA, proposed lien, notifications). Everything comes from data the page already holds; nothing is invented. Click handlers are wired once at map init, so they go through refs (`mapClickRef`, `openHotspotRef`); before, the heat-point handler kept the alert list from init time. The table-view fallback still uses HotspotDrawer. The map now fills the remaining viewport height above the bottom taskbar.
- The Vite "Empty parenthesized expression" overlay (CaseDetail.tsx) was a transient state from a mid-edit save; the file is fixed, reload clears it.
- General UI finish (operations-console look, kept light): emoji glyphs replaced by one stroke-icon set (shared/ui/Icon.tsx); opaque surfaces everywhere (rgba backgrounds/borders/text composited to solid hex in tokens.css, backdrop blur and coloured glows removed; only modal scrims, the heat layer and map dimming stay translucent); rounded corners kept (intel dossier is 12 px rounded, corner brackets dropped); no new status dots (KPI chip dot hidden); uppercase letter-spaced section headings and a slim "Synthetic data // Training environment // No real accounts" strip above the top bar; graph labels/edges opaque.
- Investigate console finish: the Network / Fund flow switch is a full-width pair of equal, emphasised buttons (`.nk-seg`, 4 px gap, active = solid accent); dossier/cluster titles and card headings use the uppercase letter-spaced style (`.nk-console-title`, `.nk-dossier-h`); cases list uses mono ids and uppercase actions/badges; cluster explorer graph is taller (640) with its legends below the canvas; "1 nodes" pluralisation fixed.
- Command page rebuilt: header with operator + sim DTG (not wall clock), a full-width "closest to closing" call to action (or a "queue is clear" note), six KPI tiles in a 3x2 grid (disputed total in compact Cr/L so it no longer clips), and two working panels — Priority alerts (live first, then by severity) and Largest networks (by disputed amount) — each row a link into the alert / cluster. Taskbar brand: the "NK" mark is removed; "NAKABANDI" is a square-cornered bordered button back to Command.
- Demo priming: `scripts/demo_prime.py` (real ingest API only) raises fresh complaints from new mule accounts whose home facility is a real branch/ATM (one per district across the four states; forecast confidence is 1/(facilities of that bank in the victim's district), so only banks with 1-2 facilities there yield alerts), acknowledges a few as an analyst, and `--tick` keeps the simulation clock running at 1x so countdowns tick and windows last their full 75 min. Each prime run advances the sim clock ~30 min: stop any running `--tick` process before priming again, then restart it. Long-running seeded clusters cash out over dozens of places, so they (rightly) abstain and raise no alerts.
